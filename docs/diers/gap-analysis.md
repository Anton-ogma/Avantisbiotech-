# Сопоставление: ТЗ v1.0 ↔ существующая реализация

**Цель:** ответить на вопрос «сколько из ТЗ уже сделано и что из существующего кода
переиспользуется».

**Сравниваются:**
- [ТЗ на реализацию: постуральный модуль DIERS v1.0](./TZ-postural-module-v1.0.md)
  и [исследовательский контур](./research-framework.md) — целевое состояние;
- [модуль DIERS в `fitmed-ecosystem@3a028f0`](./as-is/overview.md) — фактическое состояние.

---

## 1. Вывод

Существующий модуль и ТЗ решают **разные задачи**.

Существующий модуль — хранилище снимков: одно исследование на дату, ручной ввод или CSV,
попарная арифметическая разность значений. Целевой модуль — измерительный инструмент,
построенный вокруг **пробы**: нейтраль, стандартизованные условия, Δ относительно нейтрали,
порог значимости SDC, ранжирование откликов, подтверждающий повтор, детерминированный
анализ с версионированной конфигурацией.

Центральной абстракции ТЗ — пробы (`trials`) и нейтрали — в существующем коде **нет ни в каком
виде**. Всё, что строится поверх неё (§8 протокол, §9 анализ, §11 рекомендации, §12
сопоставимость), опирается на неё, поэтому не может быть «дописано» к текущей схеме.

Переиспользуемая часть — не код, а **знание**: словарь параметров DICAM, устройство CSV-экспорта
и подтверждённый факт отсутствия у DICAM публичного API. Оценка переиспользования кода:
близко к нулю; переиспользования предметных знаний — заметная, см. §4.

---

## 2. Сопоставление по разделам ТЗ

Вердикты: **нет** · **частично** · **есть, но противоречит** · **есть**.

### §1 Границы, draft/signed

| Требование | Факт | Вердикт |
|---|---|---|
| Вывод создаётся в `draft`, подпись врачом | отчётов нет вовсе | нет |
| Водяной знак на неподписанном | — | нет |
| Версии норм/порогов/правил в отчёте | конфигурации нет | нет |
| Требования MDR (аудит, версионирование, воспроизводимость) | ничего из трёх | нет |

### §3 Стек

| Требование | Факт | Вердикт |
|---|---|---|
| Python 3.12 · FastAPI · Pydantic v2 · SQLAlchemy 2 · Alembic | TypeScript · Express · drizzle | **конфликт** |
| PostgreSQL 16 | PostgreSQL, но через обёртку `sqlite.prepare` | частично |
| Redis + ARQ | очередей нет, импорт синхронный в HTTP-запросе | нет |
| S3/MinIO | файлы не хранятся, временный файл удаляется после разбора | нет |
| React + TS + Vite | есть | есть |
| Типы фронтенда генерируются из моделей | интерфейсы дублируются вручную в обоих компонентах | нет |
| Bridge Agent | нет | нет |
| `domain` без БД/сети/часов | вся логика внутри обработчиков Express, `new Date()` в разборе | нет |

> **Ключевое следствие.** §3 прямо предписывает: если хост-приложение на другом языке —
> весь домен выносится отдельным Python-сервисом с контрактом §13, смешивать домен между
> языками запрещено. Хост (`fitmed-ecosystem`) на TypeScript. Значит существующий TS-модуль
> **не может стать** целевым; целевой домен создаётся с нуля отдельным сервисом, а fitmed
> в лучшем случае остаётся фронтом/хостом, вызывающим его по API.

### §4 Модель данных

15 таблиц ТЗ против 2 существующих.

| Таблица ТЗ | Аналог | Вердикт |
|---|---|---|
| `patients_ref` | нет; данные висят на `user_id` внутренней учётки пациента | **конфликт**, см. §3 ниже |
| `sessions` | нет | нет |
| `trials` | **нет** — ключевая абстракция отсутствует | нет |
| `raw_imports` | нет; файл не сохраняется, хэша нет, идемпотентности нет | нет |
| `measurements` | ближайший аналог — `diers_studies` с тремя JSON-полями | частично |
| `emg_recordings` / `channels` / `epochs` / `features` | `myoline_studies.emg_channels` — плоский массив без записи, каналов, эпох и синхронизации | частично |
| `condylography_recordings`, `condylar_metrics`, `probe_condylar_positions` | нет | нет |
| `leg_axis_measurements` | нет | нет |
| `analyses` (+ `input_hash`, три версии) | нет; сравнение считается на лету и не сохраняется | нет |
| `reports` | нет | нет |
| `outcomes` | нет | нет |
| `audit_log` | нет | нет |

Инварианты ТЗ:

| Инвариант | Факт |
|---|---|
| `file_hash` уникален, повторный импорт не дублирует | повторная загрузка того же файла создаёт полный дубль всех строк |
| `input_hash`, детерминизм результата | результата как сущности нет |
| Версии конфигурации — копии значений | конфигурации нет |
| `audit_log` только на вставку | лога нет |

### §5 Импорт

| Требование | Факт | Вердикт |
|---|---|---|
| Два пути (агент + ручной), единый канонический слой | только ручная загрузка | частично |
| `source` = agent\|manual | есть поле `source` = manual\|csv_import — другая семантика | частично |
| Запрет молчаливого пропуска, статус `unrecognized` | нераспознанные столбцы молча теряются; хуже — при нераспознанном заголовке весь CSV-ряд пишется в `spine_params` (Д-09) | **есть, но противоречит** |
| Парсер на версию формата с `detect()` | один жёстко зашитый CSV-разбор без определения формата | нет |
| Частота дискретизации из заголовка, флаг `resample_suspected` | нет | нет |
| Десятичный разделитель по содержимому файла | нет; `parseFloat` по локали JS | нет |
| Реальный файл-фикстура на каждый парсер | тестов нет вовсе | нет |
| Форматы: formetric XML+CSV, pedoscan CSV, ЭМГ C3D/EDF/CSV, аксиография XML | только CSV formetric и CSV Myoline | частично |
| `canonical/sign_conventions` как единственное место знаковых соглашений | знаки нигде не определены; значения хранятся строками «как пришли» | нет |
| Property-тест зеркальности | нет | нет |

### §6 Синхронизация

Уровни E, M, S — **нет ничего**. Нет непрерывной записи, маркеров, модели времени
`t_common = a·t_sensor + b`, детектора разрывов, сверки `excursion_mm`. Кондилографии нет
как модальности.

### §7 Канонизация

| Требование | Факт |
|---|---|
| `param_registry` (код, модальность, единица, домен, направление, участие в PI) | нет реестра; списки ключей зашиты константами в обработчике импорта |
| Единицы измерения | не хранятся; подписи «°» и «мм» зашиты в UI |
| Направление `two_sided`/`higher_worse`/`lower_worse` | отсутствует — и это ровно причина дефекта Д-04: рост любого параметра красится зелёным, включая `Scoliosis` |
| Обязательный минимум формометрии (13 параметров) | 6 в форме ввода + распознавание 16 ключей при импорте; названия не канонические (`Kyphosis` vs `KYPHOTIC_ANGLE_ICT_ITL`) |
| Pedoscan | нет |
| 12 флагов качества | нет ни одного |

### §8 Протокол сессии

Машина состояний, 8 блокирующих валидаторов, дрейф нейтрали, `baseline_ref`,
подтверждающий повтор — **нет ничего**. Понятия сессии не существует: исследования
создаются поштучно и независимо.

### §9 Алгоритмы анализа

| Требование | Факт | Вердикт |
|---|---|---|
| z-оценка по нормам | норм нет, z не считается | нет |
| PI, PI_domain, веса | нет | нет |
| Δ относительно `baseline_ref` | считается «A минус B» между двумя произвольными исследованиями, нейтрали нет | **есть, но противоречит** |
| `effect_size = Δ / SDC` | SDC нет, порога нет | нет |
| MUST: значения ниже порога не ранжируются и не интерпретируются | показываются все дельты без исключения, с цветовым акцентом | **прямое нарушение** |
| Оси нижней челюсти | нет | нет |
| Когерентность поза/сустав/мышца | нет | нет |
| Карта отклика | нет | нет |
| Комбинированная проба | нет | нет |
| ЭМГ: фильтрация 20–450, режекция 50 Гц, RMS-огибающая, SNR, метрики | ЭМГ хранится одним числом на мышцу, сигнала нет | нет |
| MUST: мкВ сравнивать только внутри сессии | сравниваются между любыми двумя исследованиями любых дат | **прямое нарушение** |
| MUST: myoline (сила) не участвует в анализе проб | в существующей системе «myoline» — это ЭМГ в мкВ; см. §6 ниже | **терминологический конфликт** |

### §10 Конфигурация как данные

YAML-конфигураций (нормы, пороги, реестр проб, правила), валидации схемы при старте,
обязательного `source`, предупреждения при `demo`-порогах — **нет ничего**. Все константы
(списки ключей, набор мышц, единицы) зашиты в код.

### §11 Рекомендации

Движка правил, библиотеки упражнений, логики стелек, прогноза — нет. Ни одной рекомендации
модуль не выдаёт.

### §12 История и сопоставимость

| Требование | Факт | Вердикт |
|---|---|---|
| Сравнимость только при совпадении `neutral_definition` и версий | сравниваются любые два исследования без каких-либо проверок — включая разные `device_type` (formetric4d ↔ formetric3d) | **прямое нарушение** |
| При несовпадении Δ не вычисляется, причина выводится | Δ вычисляется всегда | нарушение |
| Раздельное отслеживание `PI(нейтраль)` и `ΔPI(лучшая проба)` | нет | нет |

### §13 API

Существующие 16 эндпоинтов ориентированы на «исследование», целевые 11 — на «сессию».
Совпадений по существу нет; частичное пересечение только у экспорта и истории.

Отдельно: в §13 **отсутствуют** эндпоинты для переходов, которые §8.1 объявляет
блокирующими — подтверждение качества и подтверждение шорт-листа. См. вопрос В-7.

### §14 Интерфейс

| Требование | Факт |
|---|---|
| 12 разделов + режим тренера | 2 компонента: лента + карточка + таблица сравнения |
| Полоса ±SDC на каждом графике сравнения | графиков нет вовсе; таблица без порогов |
| Единая шкала, запрет автомасштаба | неприменимо — визуализации нет |
| Контракт цвета по направлению | цвет по знаку дельты, без семантики параметра (Д-04) |
| Нейтраль не снимается в сравнениях | нейтрали нет |
| Норма только там, где определена | норм нет |
| Различие «прочерк» и «дефект» | всё пустое выглядит одинаково |
| Парное сравнение отпечатков + карта разности | pedoscan нет |
| Светлая/тёмная/авто темы | только тёмная |
| Адаптив ПК/планшет/телефон | вёрстка мобильная, раскладок под ПК и планшет нет |
| Доступность | карточки — `div` без role/tabIndex/клавиатуры (Д-07) |

### §15 Приватность, роли, аудит

| Требование | Факт | Вердикт |
|---|---|---|
| Роли operator/clinician/methodologist/coach/admin | есть только пациент и врач; методолога, оператора, тренера-читателя нет | частично |
| ПДн не хранятся, только `patient_ref` | модуль живёт внутри общей учётной системы с полными профилями пользователей | **конфликт** |
| Аудит открытия/изменения/подписи/экспорта | нет | нет |
| Обезличенный экспорт когорты, ключ вне модуля | экспорт есть, но пациентский и с `raw_data` внутри (Д-06) | нет |
| On-prem как основной сценарий | приложение облачное | частично |

### §16 Тестирование

Ни одной строки из таблицы приёмки не покрыта: тестов на модуль нет вовсе.

---

## 3. Три архитектурных конфликта — решены

**К-1. Язык домена.** §3 запрещает смешивать домен между языками и требует Python-сервис,
если хост на другом языке. Хост — TypeScript.
✅ [Р-10](./decisions.md#р-10--язык-домена-и-граница-с-хостом): модуль — отдельный
Python-сервис со своей БД, хост обращается к нему только по §13. Существующий TS-модуль
целевым не становится.

**К-2. Владение данными пациента.** Сейчас исследование принадлежит **учётной записи
пациента**: пациент сам создаёт, редактирует и удаляет свои исследования. ТЗ строит
клинический инструмент: данные вводит оператор, подписывает врач, ПДн в модуль
не попадают (§13, §15), удаление противоречит аудиту (§15) и воспроизводимости (§4).
✅ [Р-11](./decisions.md#р-11--владение-данными-пациента): пациент не является субъектом
записи; получает только пациентский вариант подписанного отчёта.
**Это продуктовое изменение** — сценарий «пациент ведёт свои исследования сам»
в целевой системе не воспроизводится.

**К-3. Что такое myoline.** В ТЗ (§9.8) myoline — изометрическая сила, в анализе проб
не участвует; ЭМГ — отдельная модальность. В существующей системе таблица
`myoline_studies` хранит **ЭМГ в мкВ**.
✅ [Р-12](./decisions.md#р-12--что-такое-myoline): термин закрепляется за силой,
legacy-данные при миграции трактуются как ЭМГ. Обратный вариант сломал бы MUST §9.8.

---

## 4. Что из существующего переиспользуется

Честный список — короткий, но не пустой.

1. **Словарь параметров DICAM.** 16 спинальных, 7 постуральных, 6 gait-ключей и карта
   12 мышц ЭМГ — это результат работы с реальным экспортом. Прямой стартовый материал
   для `param_registry` (§7) и для маппинга в канонические коды.
   Источник: [Приложение В](./as-is/annex-c-csv-mapping.md).
2. **Устройство CSV-экспорта DICAM.** Разделители `;`/`,`/таб, немецкие заголовки
   (`Datum`, `Titel`), кавычки — фактура для парсера `formetric-csv` и его фикстур (§5.2).
3. **Подтверждённое отсутствие публичного API у DICAM.** Проверено и задокументировано;
   обосновывает выбор Bridge Agent, следящего за папкой экспорта (§5.1), вместо попыток
   сетевой интеграции. Существующий эндпоинт `api-sync` — задокументированная заглушка
   с перечнем реальных путей интеграции, её содержание переносится в документацию.
4. **Схема доступа врача к данным пациента** через проверку наличия приёма — рабочий
   прототип гейтинга, но в целевой ролевой модели (§15) требует переработки.
5. **UI-паттерны** ленты и таблицы сравнения — визуальная отправная точка, не более:
   в целевом виде обязательны полоса ±SDC, единая шкала и семантика направления (§14.2).

Не переиспользуется: схема БД, слой импорта, логика сравнения, экспорт, API-контракт.

---

## 5. Что блокирует старт и имеет длинный срок поставки

Это не задачи разработки, но без них соответствующие этапы не закрываются приёмкой.

| # | Что нужно | Что блокирует | Почему нельзя отложить |
|---|---|---|---|
| ~~Б-1~~ | ~~SDC — repeatability-исследование клиники~~ → **снят** [Р-20](./decisions.md#р-20--собственные-нормы-и-пороги-этап-15): платформа считает собственный SDC, добавлен Этап 1.5 | — | внешнего источника для этой комбинации оборудования не существует; SDC становится функцией платформы, а не условием разработки |
| ~~Б-2~~ | ~~Нормы со стратификацией~~ → **снят** [Р-13](./decisions.md#р-13--индекс-отзывчивости-ri-работа-без-норм) + [Р-20](./decisions.md#р-20--собственные-нормы-и-пороги-этап-15): введён `RI`, норм не требующий; нормы порождаются из своей когорты | — | принять чужие нормы значило бы встроить в результат чужое допущение |
| Б-3 | **Реальные файлы экспорта** каждого формата: formetric XML и CSV, pedoscan CSV, ЭМГ C3D/EDF/CSV, аксиография XML (§5.2 MUST) | Этапы 1, 3, 4, 5 | фикстура-требование сформулировано как MUST; парсер без реального файла не принимается |
| Б-4 | **Знаковые соглашения по версиям формата** (§5.3) | Этап 1 | property-тест зеркальности пишется **до** первого парсера |
| Б-5 | **DPIA и правовое основание** (§15) | пилот | оформляются до пилота, не после |
| Б-6 | **Решение по MDR-классу** (§1) | Этап 6 | определяет требования к движку рекомендаций и к документации |
| Б-7 | **Веса доменов `W_domain`** — профиль клиники (§9.2) | Этап 2 | PI без весов не считается |

После Р-13 и Р-20 внешним блокером остаётся **Б-3** — реальные файлы экспорта каждого
формата. Он требует работы на приборе, а не в коде, и запускается параллельно с Этапом 1.

Б-1 и Б-2 сняты не отменой требований, а сменой источника: платформа порождает пороги
и нормы из собственных данных, а до их появления работает на `RI` и помечает
конфигурацию как `demo`.

---

## 6. Судьба существующего модуля и данных

**Код.** Развить в целевой нельзя (К-1, отсутствие `trials`). Варианты: оставить как legacy
только на чтение до появления Этапа 1, либо погасить. Незакрытые дефекты
([Приложение Г](./as-is/annex-d-defects-backlog.md)) при сценарии «оставить на чтение»
сокращаются до двух значимых: Д-06 (`raw_data` в пациентском экспорте) и Д-13
(doctor-эндпоинт без проверки связи с пациентом). Д-01 и Д-02 при сценарии погашения
теряют смысл — чинить их сейчас имеет смысл только если модуль остаётся в проде надолго.

**Данные.** Существующие исследования не содержат ни кода пробы, ни нейтрали, ни версий
конфигурации, ни `settle_sec`, ни платформенной компенсации. По §12 они несопоставимы
ни с чем, включая друг друга. Максимум, что корректно: перелить их в `raw_imports` +
`measurements` с `trial = null`, пометкой «до-протокольные» и полным исключением из анализа
и из истории. Претендовать на роль базовой точки курса они не могут.

**Терминология.** До миграции решить К-3 — иначе legacy-ЭМГ окажется в таблицах myoline.

---

## 7. Вопросы к автору ТЗ — закрыты

Все девять вопросов решены, решения и правки в ТЗ — в [журнале](./decisions.md).

| # | Вопрос | Решение |
|---|---|---|
| В-1 | два словаря состояний сессии | [Р-1](./decisions.md#р-1--состояние-сессии--одно-поле) — одно поле, домен §8.1 |
| В-2 | переанализ при смене версий | [Р-2](./decisions.md#р-2--переанализ-при-смене-версий) — `analyses` только на вставку, переанализ штатной операцией |
| В-3 | профиль весов в хэше | [Р-3](./decisions.md#р-3--пять-версий-конфигурации) — пять версий: + `profile`, + `registry` |
| В-4 | порядок проверки в §9.7 | [Р-4](./decisions.md#р-4--порядок-вердиктов-комбинированной-пробы) — явный приоритет, + `synergistic`, + `indeterminate` |
| В-5 | ранжирование по модулю | [Р-5](./decisions.md#р-5--два-списка-вместо-ранжирования-по-модулю) — `shortlist` только улучшающие, ухудшающие отдельно |
| В-6 | `conflict` в двух значениях | [Р-6](./decisions.md#р-6--переименование-conflict-в-когерентности) — §9.5 → `posture_muscle_conflict` |
| В-7 | эндпоинты блокирующих переходов | [Р-7](./decisions.md#р-7--эндпоинты-переходов-состояния) — 3 эндпоинта, явные и неявные переходы |
| В-8 | историзм `neutral_definition` | [Р-8](./decisions.md#р-8--историзм-neutral_definition) — копия в `sessions`, подтверждено |
| В-9 | граница с fitmed | [Р-9](./decisions.md#р-9--требования-к-patient_ref) — ключ у хоста, `patient_ref` неперечислим |

Пять решений расширяют ТЗ сверх заданного вопроса (новые поля, значения, эндпоинты) —
в журнале помечены **⚠ расширение** и отменяемы независимо друг от друга.

---

## 8. Что поменялось в ТЗ

Разделы 4–12 — контракт, меняются только через ревью (§0 ТЗ). Внесённые правки:

| Раздел ТЗ | Правка | Решение |
|---|---|---|
| §3 | граница с хостом: отдельный Python-сервис | Р-10 |
| §4 `sessions` | `status not null` с доменом §8.1; `low_confidence`; `neutral_definition`; `profile_version`, `registry_version` | Р-1, Р-3, Р-8 |
| §4 `analyses` | `profile_version`, `registry_version`; только на вставку | Р-2, Р-3 |
| §4 инварианты | 2 переписан на пять версий; добавлены 5 и 6 | Р-2, Р-3, Р-8 |
| §7 | версионирование `param_registry` | Р-3 |
| §8.1 | терминальный `reported`, односторонность, разграничение с `reports.status` | Р-1 |
| §8.3 | два списка вместо ранжирования по модулю | Р-5 |
| §9.5 | `conflict` → `posture_muscle_conflict` | Р-6 |
| §9.7 | нормативный порядок, + `synergistic`, + `indeterminate` | Р-4 |
| §9.8 | трактовка legacy-данных myoline | Р-12 |
| §10 | конфигурации `profiles/` и `registry/` | Р-3 |
| §12 | состав проверки, сверка по `analyses`, подбор набора версий | Р-2, Р-3, Р-8 |
| §13 | 3 эндпоинта перехода, `reanalyze`, `versions`, MUST к `patient_ref` | Р-2, Р-7, Р-9 |
| §15 | MUST о ролях записи | Р-9, Р-11 |
