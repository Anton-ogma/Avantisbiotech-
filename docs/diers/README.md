# Постуральный модуль DIERS — документация

## Состав

| Документ | Что это |
|---|---|
| [**ТЗ на реализацию: постуральный модуль DIERS v1.0**](./TZ-postural-module-v1.0.md) | целевое техническое задание, 18 разделов: границы, стек, модель данных, импорт, синхронизация, канонизация, протокол, алгоритмы, конфигурация, рекомендации, история, API, интерфейс, приватность, тестирование, этапы |
| [**Сопоставление ТЗ ↔ реализация**](./gap-analysis.md) | что из ТЗ уже есть, что переиспользуется, три архитектурных конфликта, семь блокеров с длинным сроком, девять вопросов к автору ТЗ |
| [**Журнал решений**](./decisions.md) | 12 решений (Р-1…Р-12), закрывающих все открытые вопросы и архитектурные конфликты: что решено, почему, что поправлено в ТЗ |
| [Существующий модуль в fitmed-ecosystem (as-is)](./as-is/README.md) | описание унаследованной реализации: обзор + 4 приложения (модель данных, API, формат CSV/DICAM, реестр дефектов) |

## Что важно знать сразу

**Существующий модуль и ТЗ решают разные задачи.** Унаследованный модуль — хранилище
снимков: одно исследование на дату, попарная разность значений. Целевой — измерительный
инструмент вокруг **пробы**: нейтраль, стандартизованные условия, Δ от нейтрали, порог
значимости SDC, ранжирование откликов, детерминированный анализ с версионированной
конфигурацией.

Центральной абстракции ТЗ — пробы (`trials`) — в существующем коде нет ни в каком виде.
Переиспользуется не код, а предметные знания: словарь параметров DICAM, устройство
CSV-экспорта, подтверждённое отсутствие у DICAM публичного API.

**Открытых вопросов по ТЗ нет.** Девять вопросов и три архитектурных конфликта закрыты
решениями Р-1…Р-12; ТЗ поправлено в §3, §4, §7, §8, §9, §10, §12, §13, §15. Пять решений
расширяют ТЗ сверх заданного вопроса — в журнале помечены **⚠ расширение** и отменяемы
по отдельности. Одно решение (Р-11, пациент не пишет в модуль) меняет продуктовую модель,
а не только схему прав.

**Два блокера с самым длинным сроком** — SDC-исследование повторяемости в клинике (без него
не закрывается Этап 2 целиком) и реальные файлы экспорта каждого формата как фикстуры
(§5.2 MUST). Оба требуют работы на приборе, а не в коде, — запускать параллельно с Этапом 1.
