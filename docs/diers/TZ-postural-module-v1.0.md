# ТЗ на реализацию: постуральный модуль DIERS

Версия 1.0 · документ для разработки. Заменяет ранние черновики в части реализации;
методические обоснования остаются в отдельных документах и здесь не повторяются.

---

## 0. Как читать этот документ

- **MUST** — обязательно, без этого приёмка не проходит.
- **SHOULD** — сильная рекомендация, отклонение фиксируется в решении.
- **MAY** — на усмотрение команды.
- Разделы 4–12 — контракт данных и алгоритмов, менять только через ревью.
- Все числовые пороги и нормы в примерах — **демонстрационные**. Продуктовые значения
  приходят из конфигурации (§10) и в код не зашиваются никогда.

---

## 1. Границы системы

Система — **научно-практическая платформа**: она предназначена выявлять закономерности,
а не применять готовые. Комбинация оборудования и целей прецедента не имеет, протоколы
разрабатываются здесь же. Нормы, пороги, направление «лучше/хуже» и правила —
параметры исследования, а не константы; каждое поддерживает состояние «не определено»
и своим отсутствием работу платформы не блокирует.

MUST (Р-19): развёртывание имеет явный режим назначения `intended_use ∈ {research, clinical}`.
В режиме `research` движок правил (§11) отключён — платформа выдаёт наблюдения и
статистику, но не терапевтические назначения. В режиме `clinical` правило загружается,
только если опирается на параметры с установленным направлением и на закономерности,
прошедшие подтверждающий анализ.

Требования к дизайну исследования — предрегистрация, слепая оценка качества,
sham-условие, контроль множественности, lockbox — вынесены в
[исследовательский контур](./research-framework.md) и обязательны наравне с настоящим
документом.

В клиническом применении система остаётся **инструментом поддержки решений врача**.
Она не ставит диагноз и не назначает лечение автономно.

MUST:
- любой вывод (отклонения, ранжирование проб, рекомендации, прогноз) создаётся в статусе `draft`;
- переход в `signed` — подписание **усиленной квалифицированной электронной подписью**
  медицинского работника (ФЗ-63; Приказ МЗ РФ № 947н). Подписывается хэш сформированного
  документа, а не запись в БД; подпись хранится с документом и проверяется при каждом
  открытии (Р-22);
- отчёт в статусе `draft` не выгружается пациенту и не печатается без водяного знака;
  медицинским документом не является;
- в каждом отчёте печатаются версии норм, порогов и правил, а также режим `intended_use`.

Регуляторный контекст (Р-21, Р-25): платформа разрабатывается и эксплуатируется в РФ,
применяется законодательство РФ и право ЕАЭС. ПО, предназначенное для диагностики,
профилактики, лечения или мониторинга, является **медицинским изделием** (ФЗ-323, ст. 38)
и подлежит государственной регистрации (ПП РФ № 1416; Решение Совета ЕЭК № 46).
ПО исключительно для научных исследований медицинским изделием не является.

Поэтому `intended_use` — не флаг функциональности, а **регуляторная граница**:
`research` — не медизделие, регистрация не требуется; `clinical` — медизделие,
эксплуатация без регистрационного удостоверения запрещена. Сборка не стартует
в `clinical` без настроенного номера РУ.

Требования §15 (аудит, версионирование, воспроизводимость) нельзя откладывать «на потом»
в обоих режимах. Полная развёртка правовых требований — [нормативная рамка РФ](./regulatory-rf.md).

Вне рамок первой версии: биофидбек в реальном времени, интеграция с ЭМР клиники, мобильное
приложение пациента, автоматическое проектирование стелек.

---

## 2. Глоссарий и коды

| Термин | Значение |
|---|---|
| Проба (probe) | стандартизованное условие обследования: положение НЧ, аппарат, клин |
| Нейтраль | опорная проба, относительно которой считаются все Δ |
| Sham | методически неактивное условие, неотличимое для пациента от активного |
| PI | постуральный индекс, сводная удалённость от нормы; требует норм |
| RI | индекс отзывчивости, сводная величина отклика в единицах SDC; норм не требует |
| SDC | наименьшее выявляемое изменение параметра |
| Effect size | Δ / SDC |
| Домен | группа параметров: `frontal`, `pelvis`, `rotation`, `sagittal`, `support`, `leg_axis` |
| Когерентность | согласованность изменения по позе, суставу и мышце |

Коды проб (справочник, расширяем через конфигурацию):

```
MAND_NEUTRAL_NO_APPARATUS   MAND_NEUTRAL_WITH_APPARATUS
MAND_CLENCH  MAND_PROTRUSION  MAND_RETRUSION
MAND_LAT_LEFT  MAND_LAT_RIGHT  MAND_OPEN
MAND_SPLINT_JOINT  MAND_SPLINT_THERAPEUTIC  MAND_COTTON_ROLLS
PODAL_WEDGE_L  PODAL_WEDGE_R  PODAL_HEEL_LIFT  PODAL_INSOLE_PROTO  PODAL_BAREFOOT_REF
CTRL_TASK_NONORAL  CTRL_TONGUE  CTRL_SHAM_<уровень>
COMBI_<A>_PLUS_<B>
```

`CTRL_SHAM_*` добавлен решением Р-15: без sham изменение позы под пробой объясняется
временем, вниманием и ожиданием эффекта. Повторная нейтраль контролирует дрейф,
но не сам факт вмешательства.

---

## 3. Стек и структура

MUST:
- бэкенд Python 3.12 + FastAPI + Pydantic v2 + SQLAlchemy 2 + Alembic;
- БД PostgreSQL 16;
- очередь Redis + ARQ;
- объектное хранилище S3-совместимое (MinIO on-prem);
- фронтенд React + TypeScript, сборка Vite;
- типы фронтенда генерируются из Pydantic-моделей, ручное дублирование запрещено;
- Bridge Agent — Python + watchdog, сборка PyInstaller, служба Windows.

Если хост-приложение написано на другом языке, весь домен выносится отдельным Python-сервисом
с контрактом §13; смешивать домен между языками запрещено.

Р-10: хост — `fitmed-ecosystem` (TypeScript/Express), поэтому действует именно этот вариант.
Модуль реализуется отдельным Python-сервисом со своей БД и разворачивается самостоятельно;
хост обращается к нему только по §13. Существующий TS-модуль DIERS в fitmed целевым
не становится и в кодовую базу модуля не переносится.

```
apps/  web · api · worker-signal · worker-report · bridge-agent
packages/
  domain/      canonical · protocol · analysis · rules · report   ← чистые функции
  importers/   formetric · pedoscan · myoline · emg · condylography
  contracts/   Pydantic + генерация TS
  config/      norms · thresholds · probes · rules   (YAML, версионируется)
analytics/     Python-ноутбуки, НЕ связаны с рантаймом
db/migrations/
tests/         fixtures · golden
```

MUST: `packages/domain` не обращается к БД, сети и системным часам. Всё приходит аргументами.

---

## 4. Модель данных

```sql
patients_ref(
  patient_ref        text primary key,      -- идентификатор из хоста, ПДн нет
  neutral_definition text not null,         -- physiological_rest | habitual_occlusion
  created_at         timestamptz not null
);

sessions(
  id uuid pk, patient_ref text fk, protocol_version text,
  started_at timestamptz, operator_ref text,
  study_mode boolean default false,
  randomization_scheme text,                 -- none|random_within_group|counterbalanced (Р-16)
  randomization_seed bigint,                 -- последовательность воспроизводима из seed
  patient_blinded boolean, operator_blinded boolean,   -- фактическая слепота, не предполагаемая
  neutral_definition text not null,          -- копия значения на момент сессии (Р-8)
  lld_mm numeric,                            -- разница длины ног
  platform_config_baseline jsonb,            -- компенсация, действующая на всю сессию
  norms_version text, thresholds_version text, rules_version text,
  profile_version text, registry_version text,   -- Р-3: веса и границы категорий; param_registry
  status text not null,                      -- единственное поле состояния, домен = машина §8.1:
                                             -- prepared | baseline_recorded | probes_running |
                                             -- imported | quality_reviewed | shortlist_confirmed |
                                             -- analyzed | reported
  low_confidence boolean default false       -- дрейф нейтрали выше порога (§8.2); ортогонален status
);
-- Решение Р-1: состояние сессии хранится в одном поле; прежний домен
-- draft|ready|analyzed|reported упразднён. См. decisions.md.

session_plans(                               -- Р-29
  id uuid pk, session_id uuid fk,
  probes jsonb not null,                     -- запланированный список: код, роль, позиция
  randomization jsonb,                       -- схема и seed, применённые к плану
  approved_by text, approved_at timestamptz
);

protocol_deviations(                         -- Р-29, ГОСТ Р ИСО 14155
  id uuid pk, session_id uuid fk, trial_id uuid,
  kind text,                                 -- missing|extra|out_of_order|settle|washout
  reason text not null, recorded_by text, recorded_at timestamptz
);

trials(
  id uuid pk, session_id uuid fk, probe_code text, ordinal int,
  pass_no int not null default 1,            -- проход; подтверждающий повтор = 2 (Р-28)
  role text,                                 -- reference|diagnostic|therapeutic|control|podal
  mode text,                                 -- static|dynamic
  is_baseline boolean,
  settle_sec_actual int, washout_sec_actual int,
  excursion_mm numeric,                      -- обязательно для эксцентрических проб
  effort jsonb,                              -- {target_pct, achieved_pct, verified_by}
  platform_config jsonb,
  started_at timestamptz
);

raw_imports(
  id uuid pk, session_id uuid fk,
  file_hash text unique not null,            -- идемпотентность импорта
  modality text, format_id text, device_sw_version text,
  source text,                               -- agent|manual
  blob_ref text, created_at timestamptz
);

measurements(
  id uuid pk, trial_id uuid fk, modality text,
  params jsonb,                              -- {code: {value, unit, sd?}}
  frames jsonb,                              -- покадровые значения, если доступны
  quality_flags jsonb, raw_import_id uuid fk
);

emg_recordings(id uuid pk, session_id uuid fk, source_id text, transport text,
  sample_rate_nominal int, sample_rate_actual int, sync_method text, sync_quality numeric,
  raw_ref text, canonical_ref text);
emg_channels(id uuid pk, recording_id uuid fk, sensor_serial text, muscle_code text,
  side text, placement_ref text, clock_a numeric, clock_b numeric);
emg_epochs(id uuid pk, recording_id uuid fk, trial_id uuid fk,
  t_start_common numeric, t_end_common numeric, flags jsonb);
emg_features(id uuid pk, epoch_id uuid fk, channel_id uuid fk, metric_code text, value numeric);

condylography_recordings(id uuid pk, session_id uuid fk, device_model text, sw_version text,
  reference_plane text, reference_position_type text, side_convention text,
  apparatus_type text, import_path text, raw_ref text);
condylar_metrics(id uuid pk, recording_id uuid fk, side text, metric_code text, value numeric);
probe_condylar_positions(id uuid pk, trial_id uuid fk, side text,
  x_mm numeric, y_mm numeric, z_mm numeric,
  source text,                               -- path|manual
  excursion_mm numeric, match_status text);  -- matched|mismatch|absent

leg_axis_measurements(id uuid pk, session_id uuid fk, side text,
  varus_valgus_deg numeric, intercondylar_mm numeric, intermalleolar_mm numeric);

analyses(id uuid pk, session_id uuid fk,
  norms_version text, thresholds_version text, rules_version text,
  profile_version text, registry_version text,
  input_hash text not null,                  -- детерминизм, sha256 по пяти версиям (Р-3)
  result jsonb, created_at timestamptz);
-- Р-2: строки analyses неизменяемы и не удаляются; переанализ создаёт новую строку.
-- Версии в analyses — то, с чем считали; версии в sessions — то, что действовало
-- в момент обследования. Сопоставимость визитов (§12) сверяется по analyses.

reports(id uuid pk, analysis_id uuid fk, status text, variant text,
  pdf_ref text, signed_by text, signed_at timestamptz);

outcomes(id uuid pk, patient_ref text, baseline_analysis_id uuid,
  followup_analysis_id uuid, predicted jsonb, actual jsonb, compliance numeric);

audit_log(id bigserial pk, actor text, action text, entity text, entity_id uuid,
  before_hash text, after_hash text, ts timestamptz);
```

Таблицы исследовательского контура — `hypotheses`, `cohorts`, `cohort_analyses`,
`exclusions` — определены в [дополнении](./research-framework.md#11-модель-данных-дополнения)
и входят в контракт наравне с перечисленными.

Инварианты (MUST, проверяются тестами):
1. `raw_imports.file_hash` уникален — повторная загрузка не создаёт второе обследование.
2. `analyses.input_hash = sha256(канонические данные + пять версий конфигурации:
   norms, thresholds, rules, profile, registry)`; одинаковый вход даёт побайтово
   одинаковый `result` (Р-3).
3. Версии конфигурации в `analyses` и `sessions` — **копии значений**, не ссылки на «текущее».
4. `audit_log` — только вставка; UPDATE и DELETE запрещены правами БД.
5. `analyses` — только вставка; переанализ порождает новую строку, прежние сохраняются.
   Подписанный отчёт остаётся привязан к своей строке `analyses` навсегда (Р-2).
6. `sessions.neutral_definition` — копия значения `patients_ref` на момент создания
   сессии; последующая смена у пациента прошлые сессии не переписывает (Р-8).

---

## 5. Импорт

### 5.1 Источники

MUST поддерживаться два пути, оба приводящие в один канонический слой:

- **Агент**: Bridge Agent следит за настроенными папками на приборных ПК, считает хэш,
  ставит файл в очередь, переживает отсутствие сети (локальная очередь с повтором).
- **Ручная загрузка**: форма в веб-интерфейсе, те же форматы, та же очередь.

Каждый импорт хранит `source` (agent|manual). Молчаливый пропуск файла запрещён:
нераспознанный формат остаётся в очереди со статусом `unrecognized` и причиной.

### 5.2 Парсеры

Контракт:

```python
class Parser(Protocol):
    format_id: str            # 'formetric-xml-v4'
    modality: str
    def detect(self, blob: bytes) -> bool: ...
    def parse(self, blob: bytes) -> CanonicalMeasurement | RawSeries: ...
```

MUST:
- один парсер на версию формата, выбор через `detect`;
- фактическая частота дискретизации берётся из заголовка файла и сравнивается с заявленной;
  расхождение — флаг `resample_suspected`;
- десятичный разделитель определяется по содержимому файла, не по локали сервера;
- на каждый парсер в `tests/fixtures` лежит реальный файл и ожидаемый разбор.

Поддерживаемые форматы первой версии: formetric XML и CSV, pedoscan CSV, ЭМГ C3D / EDF / CSV
вендора, аксиография XML, плюс форма ручного ввода сводных показателей аксиографии.

### 5.2б Многораздельный протокол (Р-43)

Печатный отчёт содержит **по разделу на условие**: заголовок `Parameter_F4_*`
повторяется для каждой пробы.

MUST: разбор режет документ на разделы по заголовку и возвращает результат на
каждый — со своей меткой условия, своими значениями и своими иллюстрациями.
Разбор документа целиком даёт метку от первого раздела и значения от последнего;
ошибки при этом не возникает, и результат выглядит правдоподобно.

MUST: ручная привязка многораздельного файла к одной пробе отклоняется с
указанием найденных условий. Разделы сняты при разных условиях, и сложенные в
одну пробу они смешиваются молча.

### 5.2а Иллюстрации протокола (Р-40)

Печатный протокол несёт растровые вложения, и часть значений напечатана **только**
на них — вне текстового слоя. Молчаливая потеря запрещена так же, как для
неканонических столбцов (§5.1).

MUST:
- парсер возвращает вложения в `ParseResult.figures`; классификация по геометрии
  (`render` — схема или реконструкция, `caption` — напечатанное значение, `decor` —
  оформление), а не по догадке о содержании;
- при наличии `caption` разбор поднимает флаг `figure_values_not_in_text_layer`:
  на листе есть числа, которых нет в разборе;
- иллюстрации хранятся привязанными к пробе и выдаются вместе с анализом.

MUST NOT: **распознавание текста на иллюстрации.** Значение, полученное OCR, не
имеет прослеживаемости к прибору, а §5.3 требует источника и знака для каждого
числа. Значения с иллюстраций вводятся вручную отдельной записью импорта
(`manual-figure-transcription-v1`, флаг `manually_transcribed_from_figure`) и
сверяются с разбором по кодам, которые есть в обоих: расхождение останавливает
загрузку.

**Одинаковая подпись не означает одинаковую величину.** «Перекос таза» протокол
печатает и в миллиметрах (таблица), и в градусах (иллюстрация) — это разные
измеряемые величины и разные коды реестра. Пересчёт между ними запрещён, когда
для него нет измеренных входных данных.

### 5.4 Монтаж ЭМГ (Р-42)

Каталог мышц (`config/emg/muscles.yaml`) — единственный источник для кодов
реестра, принадлежности области §14.3 и распознавания меток каналов. Держать
эти три списка порознь нельзя: они расходятся при каждом пополнении.

MUST: мышца с `surface: false` не порождает кодов реестра и не допускается в
монтаж. Подписать канал глубокой мышцей значит объявить измеренным то, чего
поверхностная ЭМГ не измеряет.

MUST: **монтаж сессии** — пропись «метка провода → мышца и сторона». Без него
канал, подписанный прибором как «CH3», остаётся неопознанным; догадка о том,
что это за мышца, запрещена. Монтаж собирается из шаблона группы мышц и
правится вручную — и по метке, и по составу.

MUST: две мышцы на одну метку отклоняются — при разборе одна запись перетёрла
бы другую. Односторонний отвод допускается, но асимметрия по нему не считается,
и об этом сообщается при настройке, а не пустой колонкой в отчёте.

MUST: монтаж применяется ПОСЛЕ парсера, отдельным шагом, со следом во флагах
качества. Парсер остаётся чистой функцией от байтов: иначе воспроизводимость §7
начинает зависеть от состояния сессии. По той же причине монтаж не участвует в
первом заходе распознавания формата — только в повторном, для файла, который не
опознал никто.

### 5.3 Знаки и единицы

MUST: модуль `canonical/sign_conventions` — единственное место, где задаются знаковые соглашения
и единицы по версиям формата. Property-тест: зеркальный набор входных данных даёт зеркальный
результат по всем знаковым параметрам. Этот тест пишется до первого парсера.

---

## 6. Синхронизация

Три уровня, реализуются независимо:

| Уровень | Точность | Механизм | Обязательность |
|---|---|---|---|
| E — эпохи | ±150 мс | программные маркеры, общие часы | MUST |
| M — семантический | нет времени | сопоставление по коду пробы | MUST |
| S — сэмплы | ±5 мс | аппаратный триггер / общий аналоговый канал | MAY, этап 4 |

### 6.1 Уровень E (ЭМГ, pedoscan)

- одна непрерывная запись на сессию, эпохи нарезаются по маркерам;
- модель времени канала `t_common = a·t_sensor + b`;
- `a` и `b` оцениваются по **двум** синхрособытиям — в начале и в конце сессии;
  одно событие недопустимо;
- детектор разрывов по счётчику сэмплов; интерполяция только для разрывов < 20 мс;
  эпоха с превышением получает флаг `dropout` и исключается из анализа.

### 6.2 Уровень M (кондилография)

- сопоставление по `probe_code`;
- MUST: сверка `excursion_mm` записи аксиографии и формометрической пробы,
  допуск задаётся конфигурацией (по умолчанию ±1,0 мм);
  при превышении `match_status = mismatch`, связка не строится, флаг выводится в отчёт;
- контрольные пробы `MAND_NEUTRAL_WITH_APPARATUS` и `MAND_NEUTRAL_NO_APPARATUS` обязательны,
  все кондилографические Δ считаются от нейтрали **с аппаратом**.

---

## 7. Канонизация

`param_registry` — реестр параметров: код, модальность, единица, домен, направление
(`two_sided` | `higher_worse` | `lower_worse` | `unknown`), участие в PI.

MUST (Р-18): `unknown` — штатное значение для параметра, по которому закономерность ещё
не установлена, и значение **по умолчанию** для нового параметра. Такой параметр входит
в `RI` по модулю, показывается нейтральным цветом без слов «улучшение»/«ухудшение»,
в правила рекомендаций не входит. Смена `unknown` на определённое направление — это
изменение `registry_version` и требует ссылки на подтверждающий анализ, который
направление установил. Так результат исследования попадает в продукт.

MUST: реестр версионируется (`registry_version`), входит в `input_hash` и в проверку
сопоставимости §12 — изменение домена, направления или участия параметра в PI меняет
результат при тех же измерениях (Р-3). Реестр проб (§10) версионируется отдельно,
через уже существующее `sessions.protocol_version`, и в `input_hash` не входит:
на арифметику анализа он не влияет.

Обязательный минимум формометрии: дисбаланс туловища VP-DM, сагиттальный дисбаланс,
боковое отклонение rms и max, ротация позвонков rms и max, поверхностная ротация rms,
угол кифоза ICT-ITL, угол лордоза ITL-ILS, перекос таза DL-DR, торсия таза, ротация таза,
наклон таза, наклон туловища. Полный список 40 параметров подключается по мере появления
реального экспорта.

Pedoscan: доли нагрузки лево/право, передний/задний отдел по сторонам, длина пути ЦД,
площадь ЦД, площадь контакта, индекс свода, матрица давления.

Флаги качества: `missing_landmark`, `cropped_frame`, `excess_sway`, `foot_shift`,
`resample_suspected`, `low_snr`, `clipping`, `dropout`, `line_noise_high`,
`ecg_contamination`, `motion_artifact`, `sync_uncertain`.

---

## 8. Протокол сессии

### 8.1 Машина состояний

```
prepared → plan_approved → baseline_recorded → probes_running ⇄ imported
         → quality_reviewed → shortlist_confirmed → analyzed → reported
```

Это единственное состояние сессии, хранится в `sessions.status` (решение Р-1).
Признак `low_confidence` (§8.2) ортогонален состоянию и хранится отдельным полем,
значением статуса не является. Статусы отчёта (`draft`/`signed`, §1) живут
в `reports.status` и с состоянием сессии не пересекаются.

MUST (Р-29): **план сессии** — заранее объявленный и утверждённый список проб с ролями,
порядком и точками нейтрали. Состояние `plan_approved` фиксирует утверждённый план;
до него не выполняется ни одна проба. Фактические пробы сверяются с планом, расхождения
пишутся в `protocol_deviations` с причиной — это же требование прослеживаемости
ГОСТ Р ИСО 14155.

MUST (Р-28): переходы односторонние, **кроме** цикла `probes_running ⇄ imported`.
Цикл разрешён, пока не исчерпано запланированное число проходов; проба несёт `pass_no`.
Подтверждающий повтор (§8.3) — второй проход, иначе он невыразим в машине состояний.
Промежуточный расчёт для выбора проб к повтору помечается `interim`, в `analyses`
не пишется и в отчёт не попадает.

MUST (Р-28): промежуточный расчёт выдаёт оператору **только упорядоченный перечень
кодов проб к повтору** — без величин, размеров эффекта и направления. Иначе слепота
разбора качества (Р-17) снимается до его закрытия.

Каждый переход пишется в `audit_log`.

Валидаторы (MUST). Каждый привязан к переходу; проверки полноты и порядка выполняются
**до первой пробы**, на утверждении плана — валидатор, срабатывающий после ухода
пациента, ничего не предотвращает (Р-31):

| # | Валидатор | Блокирует переход |
|---|---|---|
| 1 | `neutral_definition` задан у пациента и не меняется внутри курса; смена требует явного действия и снимает сопоставимость с прошлыми визитами | создание сессии |
| 9а | (Р-24) зафиксировано действующее ИДС (ФЗ-323 ст. 20; Приказ МЗ № 390н); документ у хоста, в модуле признак и реквизиты | **создание сессии** (Р-29) |
| 2 | порядок групп в плане: `reference → diagnostic → podal → therapeutic`; рандомизация только внутри группы; терапевтическая проба до диагностических не допускается | `prepared → plan_approved` |
| 8 | нейтраль запланирована минимум трижды: начало, середина, конец | `prepared → plan_approved` |
| 9 | (Р-15) в плане сессии со `study_mode = true`, содержащем терапевтическую пробу, есть sham соответствующего уровня | `prepared → plan_approved` |
| 10 | (Р-16) при `randomization_scheme ≠ none` зафиксирован `randomization_seed` | `prepared → plan_approved` |
| 7 | разница длины ног измерена, компенсация зафиксирована в `platform_config_baseline` | `prepared → plan_approved` |
| 5 | проверка контакта электродов пройдена | `plan_approved → baseline_recorded` |
| 3 | выдержка: фактическое `settle_sec` ≥ заданного в реестре проб | приём каждой пробы |
| 4 | washout между группами ≥ `carryover_min` | приём каждой пробы |
| 6 | для эксцентрических проб заполнен `excursion_mm`; для сжатия — `effort.achieved_pct` | приём каждой пробы |
| — | план исполнен либо расхождения объяснены в `protocol_deviations` | `imported → quality_reviewed` |
| 11 | (Р-17) решения разбора приняты вслепую: код пробы, роль, порядковый номер и результаты анализа скрыты до фиксации всех решений | `imported → quality_reviewed` |

MUST (Р-33): компенсация длины ног — **условие сессии** (`platform_config_baseline`),
а не проба. Она устанавливается до первой пробы и действует на все, включая нейтраль;
порядку групп не противоречит. Подальные пробы — дополнительные корригирующие элементы
поверх неё.

MUST (Р-29): sham вставляется **внутрь группы, которую имитирует**, и его позиция
рандомизируется наравне с активными пробами этой группы — фиксированная позиция
вычисляется и пациентом, и оператором. Контрольные пробы, не являющиеся sham
(`CTRL_TASK_NONORAL`, `CTRL_TONGUE`), относятся к группе `diagnostic`.

### 8.2 Дрейф и валидность

MUST (Р-30): дрейф считается **в пространстве параметров, нормированный на SDC** —
не через `PI` и не через `RI`:

```
baseline_drift = max_p |value(нейтраль_последняя)_p − value(нейтраль_первая)_p| / SDC_p
```

Порог — `thresholds.drift`, по умолчанию 1,0 (дрейф в пределах различимости одного
измерения). Через `PI` считать нельзя: он требует норм, которых может не быть (Р-13),
и тогда контроль валидности молча отключается именно в основном режиме платформы.
Через `RI` нельзя тоже: он определён относительно `baseline_ref`, а дрейф — свойство
самих нейтралей, получается круг.

`baseline_ref` = среднее значений нейтрали с линейной поправкой на позицию пробы во времени.

MUST (Р-30): поправка применяется всегда; `low_confidence` выставляется по **остатку
после поправки** — то есть по неучтённой, нелинейной части дрейфа. Иначе сессия
штрафуется за величину, которую сама же скомпенсировала. Исходный дрейф печатается
в отчёте как справочный.

### 8.3 Подтверждающий повтор

Подтверждающий повтор — **второй проход** сессии (Р-28): цикл
`imported → probes_running` с приращением `pass_no`. Пробы к повтору выбираются
промежуточным расчётом, который помечен `interim`, в `analyses` не пишется и оператору
выдаёт только перечень кодов без величин.

Формируются два раздельных списка (Р-5):

- **`shortlist`** — кандидаты в терапию. Только пробы с достоверным **улучшением**
  (`ΔPI < −thresholds.PI`), ранжируются по величине улучшения. После полного прохода
  топ-2 из них измеряются повторно, вперемешку с нейтралью. Проба остаётся в шорт-листе,
  только если повтор подтверждает знак и величина не выходит за порог.
  Неподтверждённые пробы в рекомендации не идут.
- **`notable_worsening`** — пробы с достоверным **ухудшением** (`ΔPI > +thresholds.PI`).
  Диагностически значимы и печатаются в отчёте, включая режим тренера (§14.5, блок
  конфликтов), но в рекомендации не попадают никогда и подтверждающим повтором
  не проверяются — помечаются `unconfirmed`.

Пробы внутри полосы шума не попадают ни в один список (§9.3).

---

## 9. Алгоритмы анализа

Все функции — чистые, без I/O. Сигнатура верхнего уровня:

```python
def analyze(session: SessionData, norms: NormsTable, thresholds: ThresholdTable) -> AnalysisResult
```

### 9.1 Отклонение от нормы

```
z = (value − norms[code].mean) / norms[code].sd
```
Категории: `|z| < 1` норма, `1 ≤ |z| < 2` пограничное, `2 ≤ |z| < 3` отклонение,
`|z| ≥ 3` выраженное. Границы задаются конфигурацией.

Для тазовых параметров используется **остаточное** значение после компенсации длины ног.

### 9.2 Индексы: PI и RI

```
PI_domain = Σ_p w_p · min(|z_p|, z_cap)          # z_cap по умолчанию 4
PI        = Σ_domain W_domain · PI_domain

RI_domain = Σ_p w_p · min(|Δ_p| / SDC_p, cap)    # отклик, нормированный на порог
RI        = Σ_domain W_domain · RI_domain
```

MUST: `leg_axis` в PI и RI не входит (структурный домен). `W_domain` — из конфигурации
профиля клиники.

MUST (Р-13): `PI` требует норм и вычисляется только для параметров, у которых нормы
определены; `RI` опирается на собственную нейтраль пациента и норм не требует.
**Отсутствие норм не блокирует ни один сценарий.** Параметр без норм участвует в `RI`
и не участвует в `PI`; колонка нормы для него — прочерк (§14.2.5), а не ноль.
В отчёте и на графиках всегда указано, какой индекс использован.

`RI` — основной индекс исследовательского контура: метод по построению измеряет отклик
на пробу, а не удалённость от популяции, поэтому популяционная норма не должна быть
входом для оценки отклика.

### 9.3 Эффект пробы

```
Δ_p            = value(trial)_p − baseline_ref_p
effect_size_p  = Δ_p / SDC_p
ΔPI            = PI(trial) − PI(baseline_ref)
```

Классификация: `|effect| < 1` шум (не интерпретируется), `1 ≤ |effect| < 2` вероятный,
`≥ 2` достоверный. Для ΔPI используется `thresholds.PI`.

MUST: значения ниже порога не участвуют в ранжировании, не попадают в шорт-лист,
не порождают рекомендаций и отображаются нейтральным цветом.

MUST (Р-14): правило выше относится к **клиническому выводу об этом пациенте**.
Подпороговые значения хранятся полностью и участвуют в исследовательском анализе на
уровне когорты. SDC — порог различимости однократного измерения у одного пациента;
ошибка групповой оценки меньше индивидуальной в √n раз, поэтому систематический сдвиг,
неразличимый в отдельном обследовании, обнаруживается на серии. Отбрасывать
подпороговые значения на входе запрещено: именно в этой зоне лежат закономерности,
ранее отвергавшиеся как «в пределах погрешности».

### 9.4 Оси нижней челюсти

```
lateral_mandible:  (MAND_LAT_LEFT, MAND_LAT_RIGHT)
sagittal_mandible: (MAND_PROTRUSION, MAND_RETRUSION)
occlusal_load:     (MAND_CLENCH, MAND_OPEN)
```

Интерпретация оси: `asymmetric_<side>_preference`, если один член пары достоверно улучшает,
а второй нейтрален или ухудшает; `symmetric_worsening`; `no_effect`, если оба в шуме.

### 9.5 Согласованность

Для каждой пробы четыре знака (Р-41): поза (`ΔPI`), сустав (симметризация координат
мыщелков), мышца (`ΔMI`), сила (`ΔSI`). Значения:

| coherence | условие |
|---|---|
| `full` | все ГОЛОСУЮЩИЕ сигналы сходятся, их не меньше двух, поза достоверна |
| `partial` | сходится хотя бы один, но не все |
| `posture_only` | поза достоверна, остальные в шуме |
| `posture_muscle_conflict` | поза улучшается, мышечная активность растёт выше порога |

MUST: `ΔMI` и `ΔSI` считаются отдельными индексами и **не суммируются** в PI.

MUST: голосуют в когерентности только сигналы с установленным направлением. Сустав
голосует всегда — §9.5 определяет его знак методически (симметризация). Сила голосует
лишь после того, как направление `MYO_FORCE_*` перестанет быть `unknown` (§9.8б).
Причина общая: «согласие» требует знать, какая сторона считается согласием, и назначать
её по умолчанию значит протащить клиническое допущение через арифметику (Р-18).

Значение переименовано из `conflict` (Р-6): слово занято вердиктом комбинированной
пробы §9.7, а в отчёте оба поля стоят рядом.

### 9.6 Карта отклика

Точки: координаты мыщелков пробы × ΔPI. MUST: интерполяция только между измеренными
соседними точками, экстраполяция запрещена и визуально штрихуется, точки в шуме
исключаются из поиска оптимума. Аппроксимация поверхностью запрещена.

Ограничения этого раздела относятся к **клиническому отображению**. В исследовательском
контуре моделирование поверхности отклика допустимо и ожидаемо, но его результат —
гипотеза (`exploratory`), которая на клинические экраны не попадает и назначением
не становится (Р-14).

### 9.7 Комбинированная проба

```
add   = ΔPI(A) + ΔPI(B)
best  = min(ΔPI(A), ΔPI(B))          # ΔPI < 0 — улучшение, поэтому лучший = минимальный

verdict — первое совпавшее условие в этом порядке (Р-4):
  conflict      если ΔPI(AB) > best + thresholds.PI      # хуже лучшего одиночного
  synergistic   если ΔPI(AB) < add  − thresholds.PI      # лучше суммы одиночных
  dominant      если |ΔPI(AB) − best| < thresholds.PI    # работает один уровень
  additive      если |ΔPI(AB) − add|  < thresholds.PI    # вклады складываются
  indeterminate иначе
```

MUST: порядок проверки нормативен — условия пересекаются, первое совпадение выигрывает.
`conflict` проверяется первым: комбинация, которая хуже лучшего одиночного уровня, остаётся
конфликтом, даже если численно совпала с суммой. `dominant` проверяется раньше `additive`:
когда вклад одного уровня в пределах шума, клинически честная формулировка — «работает
один уровень», а не «вклады сложились».

MUST: при `conflict` движок правил не выдаёт одновременное назначение обоих уровней.
При `indeterminate` комбинация не интерпретируется и в рекомендации не идёт — требуется
повтор пробы.

### 9.8а Аналитический слой (Р-35)

Четыре слоя, разные вопросы и разный приоритет. Слой 1 обязателен: без него остальные
не имеют опоры. Развёрнуто — [разбор логики](./review-consistency.md#3-аналитика--предложение).

**Слой 1. Метрология.** MUST: по каждому параметру считаются `ICC` (двусторонняя
случайная модель, абсолютное согласие), `SEM = SD·√(1−ICC)`, `SDC = 1,96·√2·SEM`,
коэффициент вариации, число пар и доверительный интервал. Результат — **паспорт
параметра**, он же закрывает требование `source` в §10. MUST: разброс между операторами
оценивается отдельно и печатается: если он сравним с эффектом пробы, выводы недостоверны.
SHOULD: регулярное измерение эталонного здорового добровольца — отделяет дрейф аппарата
и методики от изменений пациентов.

**Слой 2. Аналитика протокола.** MUST: величина отклика как функция фактической выдержки
(`settle_sec`) и времени с предыдущей пробы (`carryover_min`) — эмпирическая калибровка
реестра проб вместо назначенных чисел. MUST: **урожайность протокола** — доля проб,
давших интерпретируемый результат, против ушедших в шум и брак; она обосновывает
сокращение протокола. MUST: сводка по `protocol_deviations`, флагам качества
и `low_confidence`.

**Слой 3. Аналитика отклика.** MUST: основная единица анализа — **вектор отклика**,
а не набор скаляров. Когда эффект присутствует во многих параметрах, но каждый
по отдельности лежит у порога, поправка на множественность гасит все — а совместный
тест отвергает нулевую гипотезу. Поза есть связанная система, поэтому такая картина
здесь ожидаема, и набор одномерных тестов подавляет ровно тот сигнал, который ищется.
Выигрыш даёт агрегация согласованных сдвигов и одна проверка вместо p; учёт
ковариационной структуры (обеление, в духе Хотеллинга) — дальнейший шаг.

- MUST: многомерный перестановочный тест на векторе Δ — рядом с одномерными, не вместо;
- MUST: факторный анализ матрицы Δ → **оси отклика** (какие параметры движутся вместе);
- MUST: иерархическая (смешанная) модель — пациент как случайный эффект, проба, позиция
  в последовательности и время как фиксированные; это корректная модель для серии n-of-1;
- SHOULD: байесовский вариант с усадкой — при малом n устойчивее, даёт вероятность
  отклика для конкретного пациента без апелляции к «значимости»;
- SHOULD: типология отзывчивости — кластеризация пациентов по профилю ΔRI. Нулевой
  средний эффект при двух противоположно реагирующих подгруппах — типичный механизм,
  которым реальная закономерность объявляется отсутствующей;
- SHOULD: доза–эффект для клина, подъёма пятки, величины выдвижения;
- SHOULD: когерентность (§9.5) как непрерывная величина — корреляция Δ позы, сустава,
  мышцы и силы внутри пациента; четыре категории удобны в отчёте, но для анализа теряют
  почти всё;
- MAY: myoline как **модератор** отклика. Как сигнал он в анализе участвует (§9.8б),
  но это другой вопрос: здесь проверяется гипотеза «сильные отвечают иначе».

**Слой 4. Мета-аналитика.** MUST: сколько параметров вышло из `direction = unknown`
и по каким подтверждениям; гипотез зарегистрировано, подтверждено, отклонено, заброшено.
MUST: **калькулятор мощности на собственном SDC** — при текущем N какой размер эффекта
детектируем. `hypotheses.target_n` без него заполняется наугад, и предрегистрация
превращается в формальность.

**Инфраструктура.** MUST: воспроизводимый **снапшот когорты** в колоночном формате
с хэшем — ноутбук считает по фиксированному срезу, иначе детерминизм §7
исследовательского контура невыполним. MUST: результат из ноутбука регистрируется
обратно как `cohort_analyses` с кодом и версией. MUST NOT: BI поверх боевой БД —
это ломает и производительность, и аудит доступа §15.

### 9.8 ЭМГ

Обработка: удаление постоянной составляющей, полосовой фильтр 20–450 Гц, подавление сети
50 Гц спектральной интерполяцией, контроль ЭКГ-наводки, выпрямление, RMS-огибающая
с окном 100–250 мс.

Метрики: `RMS_MEAN`, `ASYM_LR`, `COCONTRACTION`, `MDF`, `ONSET_LATENCY` (только уровень S).

MUST: сравнение амплитуд в мкВ допускается **только внутри одной сессии** (электроды
не переклеивались). Межсессионное сравнение — только нормализованных величин; при отсутствии
нормализации блок межсессионной динамики ЭМГ не отображается вовсе.

MUST: перед интерпретацией эпохи считается SNR; при значении ниже порога канал помечается
`low_snr` и не интерпретируется.

### 9.8б Myoline — четвёртый сигнал пробы (Р-41)

Прежняя редакция: «myoline не участвует в анализе проб; используется только для
дозирования программы». **Отменено.** В этом протоколе изометрическая сила
измеряется под теми же условиями, что формометрия, ЭМГ и кондилография:
одноимённые пробы ставятся на всех четырёх приборах. Одно число на сессию
стирало бы ровно тот эффект, ради которого измерение и делается.

MUST: myoline привязывается к пробе по метке условия, как остальные модальности
(§5.1). Выгрузка **без** метки принимается, но остаётся сессионной ковариатой и
помечается `myoline_session_level_no_condition`: молча подставить её под пробу
нельзя.

MUST: сводный индекс `SI` считается отдельно и в `PI`/`RI` **не суммируется** —
складывать ньютоны с миллиметрами бессмысленно. Сила сравнивается сама с собой
между пробами.

MUST: асимметрия боковой силы `MYO_ASYM_TRUNK_LAT` считается по той же формуле
200·(R−L)/(|R|+|L|), что асимметрия ЭМГ и мыщелков. Три «асимметрии» с разной
шкалой под одним словом читались бы как одна величина.

MUST NOT: сила не участвует в вердикте и не голосует в когерентности (§9.5), пока
её направление в реестре `unknown`. «Больше силы лучше» под пробой не установлено:
рост усилия при смещённой челюсти может означать и лучшую опору, и компенсаторное
перенапряжение. Голосование включается сменой `direction` на подтверждённое
значение — то есть решением по данным, а не правкой кода.

MAY: myoline как **модератор** отклика (§9.8а, слой 3) — гипотеза «сильные
отвечают иначе» проверяется отдельно от участия силы как сигнала.

Р-12: термин закрепляется за изометрической силой; ЭМГ — отдельная модальность с
собственными таблицами (§4). Данные унаследованной таблицы `myoline_studies` в
`fitmed-ecosystem` хранят амплитуды в мкВ, то есть по существу являются ЭМГ и при
любой миграции трактуются как ЭМГ, а не как myoline.

---

## 10. Конфигурация как данные

Файлы YAML в репозитории, схема валидируется при старте — невалидный файл роняет запуск.

```yaml
# norms/norms_<version>.yaml
version: "2026.1"
source: "<обязательно: публикация или собственная выборка>"
params:
  TRUNK_IMBALANCE_VP_DM: {unit: mm, mean: null, sd: null, strata: [age_band, sex]}
```

```yaml
# thresholds/sdc_<version>.yaml
version: "2026.1"
source: "<обязательно: repeatability-исследование клиники>"
values: {TRUNK_IMBALANCE_VP_DM: null, PI: null, EMG_RMS_PCT: null}
```

```yaml
# profiles/profile_<version>.yaml   — профиль клиники (Р-3)
version: "2026.1"
z_cap: 4
z_categories: {normal: 1.0, borderline: 2.0, deviation: 3.0}
domain_weights: {frontal: null, pelvis: null, rotation: null, sagittal: null, support: null}
param_weights: {TRUNK_IMBALANCE_VP_DM: null}
```

```yaml
# registry/param_registry_<version>.yaml   — реестр параметров (Р-3)
version: "2026.1"
params:
  TRUNK_IMBALANCE_VP_DM:
    modality: formetric
    unit: mm
    domain: frontal
    direction: two_sided        # two_sided | higher_worse | lower_worse
    in_pi: true
```

```yaml
# probes/probe_registry.yaml        — версионируется через sessions.protocol_version
- code: MAND_SPLINT_THERAPEUTIC
  group: mandibular
  role: therapeutic
  settle_sec: 300
  carryover_min: 15
  modes: [static, dynamic]
```

```yaml
# rules/exercises.rules.yaml
- rule_id: R-042
  version: 3
  when:
    all:
      - param: KYPHOTIC_ANGLE_ICT_ITL
        z: {gt: 2}
      - probe_response: {group: mandibular, confidence: [reliable, probable], direction: improving}
  then:
    program_blocks: [THORACIC_EXTENSION_BASIC, DEEP_NECK_FLEXOR]
    priority: high
    requires_clinician_review: true
```

MUST (Р-20): платформа умеет **порождать** нормы и пороги из собственных данных, а не
только принимать извне. Режим повторяемости (test-retest с переустановкой пациента)
даёт `thresholds/sdc_<version>.yaml` с `source: own_repeatability`, ссылкой на когорту,
числом пар и доверительным интервалом оценки. Нормы аналогично — `source: own_cohort`
со стратификацией и числом наблюдений. Собственная норма имеет тот же статус, что и
литературная, и отличается только полем `source`. Для новой комбинации оборудования
и собственных протоколов внешнего источника попросту не существует.

MUST:
- `source` в нормах и порогах обязателен и не может быть пустым;
- пока `source` содержит маркер `demo`, интерфейс показывает предупреждение о некалиброванных
  порогах на экранах анализа, рекомендаций и в отчёте;
- каждое правило имеет эталонный кейс в `tests/golden`.

---

## 11. Рекомендации

Настоящий раздел действует только при `intended_use = clinical` (§1, Р-19). В режиме
`research` движок правил отключён: платформа выдаёт наблюдения, отклики и статистику,
но не терапевтические назначения.

MUST (Р-19): правило, опирающееся на параметр с `direction = unknown` либо на
неподтверждённую закономерность, не загружается — валидация конфигурации при старте
отклоняет такой файл. Правило нельзя написать раньше, чем закономерность подтверждена,
и это обеспечивается технически, а не регламентом.

MUST: решение принимает движок правил. LLM применяется только для формулировки текста
на основе уже принятого решения; при недоступности сервиса отчёт выпускается с шаблонными
формулировками. Обратный порядок запрещён.

MUST (Р-23): языковая модель разворачивается **в контуре организации**; в промпт
не попадают идентификаторы, `patient_ref`, даты обследований и любые комбинации значений,
позволяющие соотнести текст с лицом. Отправка во внешний сервис за пределами РФ есть
трансграничная передача специальной категории ПДн (ФЗ-152, ст. 12) и запрещена.
То же относится к телеметрии, сбору ошибок и аналитике использования — контур обработки
замкнут.

Каждая рекомендация содержит `rule_id`, `version`, перечень сработавших условий.

Логика стелек:

1. структурная разница длины ног → компенсация обязательна, величина из платформы;
2. подальная проба достоверно улучшает остаточные параметры → корригирующий элемент;
   не улучшает → стельки не показаны, даже при «неидеальной» стопе;
3. пересечение с челюстным уровнем → требуется комбинированная проба;
   при `conflict` или отсутствии пробы рекомендация становится последовательной.

Ось ноги за пределами конфигурируемого коридора → флаг `requires_orthopedic_review`;
конструкция модулем не назначается.

Прогноз выдаётся интервалом с условиями, помечается источником (`literature` | `own_cohort`)
и не выдаётся по параметрам, чьё исходное отклонение в пределах SDC.

---

## 12. История и сопоставимость визитов

MUST: два обследования сопоставимы, только если совпадают
`neutral_definition`, `norms_version`, `thresholds_version`, `profile_version`,
`registry_version`, тип опорной позиции кондилографии и тип аппарата.

`rules_version` в проверку **не входит** (Р-3): правила влияют на рекомендации,
а не на измеренные величины, и их обновление сопоставимость визитов не разрывает.

MUST: сверка идёт по строкам `analyses`, а не по `sessions` (Р-2). Версии в `sessions` —
то, что действовало в момент обследования; версии в `analyses` — то, с чем фактически
посчитали. Переанализ старых сессий под общий набор версий (§13) — штатный способ
восстановить сопоставимость после обновления норм или порогов.

`GET /patients/{ref}/history` по умолчанию подбирает **самый свежий набор версий,
доступный для всех сессий пациента**, и на нём строит динамику; набор можно задать явно
параметрами запроса. Выбранный набор печатается рядом с графиком.

При несовпадении визиты показываются рядом, но разности **не вычисляются**; интерфейс
выводит причину и предлагает переанализ, если сырые данные позволяют. Это правило
распространяется на все межвизитные величины.

Раздельно отслеживаются и никогда не смешиваются:
- `PI(нейтраль)` по визитам — динамика исходного состояния;
- `ΔPI(лучшая проба)` по визитам — динамика отзывчивости.

Порядки отображения: хронологический, по величине отклика, в порядке выбора пользователем.

---

## 13. API

```
POST   /sessions                          {patient_ref, ids_ref} -> session    (status = prepared)
POST   /sessions/{id}/plan                {probes[], randomization} -> plan_approved  (Р-29)
GET    /sessions/{id}/plan                план и фактические расхождения
POST   /sessions/{id}/trials              {probe_code, role, mode, platform_config, excursion_mm,
                                           effort, pass_no}
POST   /sessions/{id}/interim             -> перечень кодов проб к повтору, без величин (Р-28)
POST   /sessions/{id}/imports             multipart | {source: agent}
GET    /sessions/{id}/imports             очередь разбора со статусами
POST   /sessions/{id}/validate            -> список нарушений протокола
POST   /sessions/{id}/quality-review      {decisions[]} -> status = quality_reviewed      (Р-7)
POST   /sessions/{id}/shortlist           {confirmed_probe_codes[]} -> shortlist_confirmed (Р-7)
POST   /sessions/{id}/analyze             {versions?} -> analysis_id, status = analyzed
GET    /analyses/{id}                     -> AnalysisResult
GET    /analyses/{id}/report?variant=     doctor|patient|dentist|coach
POST   /analyses/{id}/report              {variant} -> reports.id, session.status = reported (Р-7)
POST   /analyses/{id}/sign                {clinician_ref, signature, signed_document_hash,
                                           timestamp_token?}   -- УКЭП, Р-22
GET    /patients/{ref}/history            визиты + признак сопоставимости; ?versions=
POST   /patients/{ref}/reanalyze          {versions, session_ids?} -> job_id              (Р-2)
POST   /outcomes                          план против факта
```

Переходы состояния сессии (§8.1) делятся на два типа (Р-7, уточнено Р-29):

- **неявные**, по факту появления данных: `plan_approved → baseline_recorded` (первая
  импортированная проба-нейтраль), `→ probes_running` (первая не-базовая проба),
  `→ imported` (очередь разбора пуста и нераспознанных файлов нет);
- **явные**, требующие решения человека: `plan`, `quality-review`, `shortlist`,
  `analyze`, `report` — каждый отдельным вызовом, каждый пишется в `audit_log`.

MUST (Р-33): `POST /analyses/{id}/report` переводит сессию в `reported` при выпуске
**первого** отчёта; последующие варианты состояние не меняют и ошибкой не являются.

MUST (Р-33): матрица «роль → допустимые варианты отчёта»:

| Роль | `doctor` | `dentist` | `patient` | `coach` |
|---|---|---|---|---|
| `clinician` | да | да | да | да |
| `operator` | нет | нет | нет | нет |
| `methodologist` | да, обезличенный | да, обезличенный | нет | нет |
| `coach` | нет | нет | нет | только свой, при согласии (Р-24) |
| `admin` | нет | нет | нет | нет |

MUST (Р-33): при `intended_use = research` варианты `patient` и `coach` **не выпускаются**:
оба содержат предписания (программа на 12 недель, «чего избегать»), а режим `research`
терапевтических выводов не даёт (Р-19, Р-25).

`POST /sessions/{id}/analyze` без `versions` берёт активный набор конфигурации;
с явным набором — считает по нему. Повторный вызов создаёт новую строку `analyses`,
прежние не трогает (§4, инвариант 5).

MUST (Р-34): публикация новой версии любой из пяти конфигураций **автоматически ставит
задачу переанализа** всех сессий — иначе история §12 деградирует при каждом изменении
реестра параметров, а изменения будут частыми (Р-18). Политика хранения: строки
`analyses`, вытесненные более свежими, не подписанные и не входящие ни в одну
зафиксированную когорту, подлежат вытеснению по конфигурируемому правилу; подписанные
и когортные хранятся всегда.

MUST: `patient_ref` — внешний идентификатор; персональные данные внутрь модуля не попадают.
MUST (Р-9): `patient_ref` непроизводен от ПДн и неперечислим — UUIDv4 либо HMAC от
идентификатора хоста с ключом, хранящимся у хоста. Последовательные идентификаторы
хост-системы в этой роли запрещены. Таблицу соответствия `patient_ref ↔ учётная запись`
ведёт хост; модуль её не хранит и за identity к хосту не обращается.

---

## 14. Интерфейс

### 14.1 Разделы

`Обзор · Сессия · Импорт · История · Анализ · Пробы · Сравнение · Нижний контур ·
Рекомендации · Отчёт · Методика · Исследование`, плюс отдельный режим «Отчёт тренеру».

### 14.2 Сквозные правила отображения (MUST)

1. **Зона шума рисуется, а не описывается.** В каждом графике сравнения присутствует
   полоса ±SDC; значения внутри неё отображаются нейтральным цветом и без акцента.
2. **Единая шкала.** Все панели одного типа используют одинаковый масштаб; автомасштаб
   на панель запрещён.
3. **Контракт цвета.** Один объект на графике — цвет кодирует направление
   (улучшение/ухудшение/шум). Несколько наложенных — цвет кодирует пробу, направление
   уходит в подпись.
4. **Нейтраль не снимается** ни в одном сравнении.
5. **Норма показывается только там, где определена.** Для координат мыщелков и амплитуд ЭМГ
   колонка нормы — прочерк.
6. **Прочерк и дефект различаются.** «Данных не предполагается» и «данные ожидались, но
   непригодны» — разные состояния с разным оформлением.
7. **Поля не накладываются.** Отпечатки стоп и матрицы давления сравниваются парно плюс
   карта разности с порогом; наложение запрещено.
8. Структурные величины (ось ноги) выводятся без Δ, с пометкой «структурный».

### 14.3 Обзор «от сустава до стоп»

Строки сверху вниз: ВНЧС и мыщелки → жевательные мышцы → шея → позвоночник фронталь →
позвоночник сагитталь → таз → ось ног → стопы. Визуально сравниваются нейтраль и первая
выбранная проба.

**Число проб в числовых колонках не ограничено** (Р-40). Прежняя редакция ставила
предел в три; предел был вёрсточный, а не содержательный, и на реальной сессии
скрывал данные, о наличии которых пользователь не узнавал. Таблица прокручивается
по горизонтали, `/compare` без параметра `probes` отдаёт все пробы сессии, отбор
столбцов делается на клиенте.

**Схемы по видам.** Для каждого вида (позвоночник фронталь, позвоночник сагитталь,
таз перекос, таз ротация, стопы) строится ряд схем: столбец — проба. Сравнивают
один вид между пробами, поэтому рядом стоят одинаковые схемы, а не набор разных
схем на каждую пробу.

MUST: схема строит только измеренное. Угол проводится под измеренным углом, размах
движения — от границы до границы, длина — в измеренном масштабе. Величина, которой
в данных нет, не изображается: правдоподобная кривая читается как измерение и им
не является. Там, где выгружены только сводные величины, показывается коридор
отклонения, а не форма кривой.

### 14.4 Мастер сессии

Пошаговый, с блокировками §8.1. Шаг качества требует решения врача по каждой строке.

### 14.5 Режим тренера

Отдельная роль отображения, только чтение. Содержит: краткое заключение простым языком,
отклонения в качественной шкале, изменения в шине, статус стелек и компенсации,
программу на 12 недель с колонкой «на что смотреть», блок «чего избегать», сроки
повторного сканирования и признаки для внепланового обращения.

MUST: скрыты пороги, размеры эффекта, координаты мыщелков, микровольты, идентификаторы
правил. MUST: блок конфликтов (например, ухудшение при сжатии зубов) отображается —
он меняет выбор упражнений.

### 14.6 Темы и адаптив

MUST:
- светлая и тёмная темы плюс режим «авто» по системной настройке;
- цвета SVG-графики берутся из той же палитры и перерисовываются при смене темы;
- шкала давления имеет отдельный набор оттенков для тёмной темы;
- раскладки: компьютер (боковая навигация), планшет до 1024 (горизонтальная навигация),
  телефон до 700 (вертикальные строки обзора, таблицы с горизонтальной прокруткой без
  сокрытия колонок).

---

## 15. Приватность, роли, аудит

Роли: `operator`, `clinician`, `methodologist`, `coach` (только чтение своего варианта отчёта),
`admin`.

MUST (Р-21):
- сведения о состоянии здоровья — **специальная категория ПДн** (ФЗ-152, ст. 10);
  до пилота оформлены модель угроз, уровень защищённости (ПП РФ № 1119), меры защиты
  (Приказ ФСТЭК России № 21), оценка вреда субъектам (ст. 18.1), назначен ответственный
  за организацию обработки, подано уведомление в Роскомнадзор (ст. 22);
- ПДн не хранятся в модуле, только `patient_ref` (требования к нему — §13, Р-9);
- аудит-лог фиксирует открытие, изменение, подпись, экспорт — **включая состав выгрузки**
  (перечень `patient_ref` и сессий): без этого не уложиться в сроки уведомления
  об инциденте, 24 часа на факт и 72 на результаты расследования (ФЗ-152, ст. 21);
- экспорт когорты обезличен, ключ соответствия хранится вне модуля;
- **развёртывание только в РФ** — on-prem либо российский хостинг. Локализация баз данных
  обязательна (ФЗ-152, ст. 18 ч. 5), альтернатив нет (Р-23);
- врачебная тайна (ФЗ-323, ст. 13): роль `coach` — не медицинский работник, тренерский
  вариант отчёта не выдаётся без отдельного согласия пациента на передачу этому лицу (Р-24);
- отзыв согласия (ФЗ-152, ст. 9 ч. 2) исполняется уничтожением соответствия на стороне
  хоста: данные модуля становятся обезличенными, исследовательский массив сохраняется,
  восстановление соответствия невозможно (Р-24).

MUST (Р-11): пациент не является субъектом записи в модуль. Данные вводит `operator`,
выводы подписывает `clinician`. Пациент получает результат только пациентским вариантом
подписанного отчёта (§13, `variant=patient`) и не имеет ни записи, ни удаления —
удаление противоречит аудиту (§15) и воспроизводимости (§4). Роль `coach` — чтение
своего варианта отчёта, без доступа к сырым величинам (§14.5).

---

## 16. Тестирование и приёмка

| Уровень | Обязательное покрытие |
|---|---|
| Фикстуры парсеров | реальный файл каждого формата → ожидаемый разбор |
| Property-тест знаков | зеркальный вход даёт зеркальный выход |
| Табличные тесты анализа | z, PI, effect size, оси, когерентность, комбинированная проба |
| Эталонные кейсы правил | сценарий → набор сработавших `rule_id` |
| Детерминизм | одинаковый вход и версии → одинаковый `input_hash` и `result` |
| Валидаторы протокола | каждое нарушение §8.1 блокирует переход |
| Сопоставимость визитов | несовпадение версий или опоры → Δ не вычисляется |
| Идемпотентность импорта | повторный файл не создаёт обследование |
| Работа без норм | сценарии проходят при пустых нормах; `RI` считается, `PI` — прочерк |
| Предрегистрация | подтверждающий анализ невозможен после доступа к lockbox |
| Множественность | размер семейства сравнений совпадает с числом фактически проверенных |
| Обратимость исключений | пересчёт на полном наборе даёт результат чувствительности |
| Слепота разбора качества | экран не отдаёт код пробы до фиксации решений |
| Подпись УКЭП | проверка подписи и сертификата при открытии; исправление — новым документом |
| Замкнутость контура | ни один сценарий не обращается к внешним хостам; ПДн в промпт LLM не попадают |
| Согласия | сессия без ИДС не выходит из `prepared`; сессия без согласия на исследование не входит в когорту |
| Отзыв согласия | уничтожение соответствия делает данные необратимо обезличенными |
| Интерфейс | каждый раздел отрисовывается; отсутствие данных даёт объяснение, а не пустой экран |
| Доступность | контраст, фокус с клавиатуры, подписи к графикам |

Критерии приёмки этапа считаются выполненными только при зелёных тестах соответствующих строк.

MUST (Р-27): таблица выше — часть **матрицы прослеживаемости** по ГОСТ Р МЭК 62304:
каждое MUST-требование имеет идентификатор, тест и ссылку на реализацию. Дополнительно
ведутся файл менеджмента риска (ГОСТ Р ИСО 14971) и перечень SOUP с версиями и обоснованием
пригодности. Класс безопасности ПО по 62304 определяется до Этапа 6; ожидаемо класс B —
решение принимает врач, а не система. Восстановить эти документы задним числом при
регистрации нельзя, поэтому они ведутся с Этапа 1.

---

## 17. Этапы

**Этап 1 — скелет данных (Р-32).** Реестр параметров (`direction = unknown` по умолчанию)
и знаковые соглашения. Парсер formetric под реальный файл. Импорт с идемпотентностью
и полным сохранением сырых данных. Сущности `sessions`, `trials`, `measurements`,
`raw_imports` — ведение сессии вручную, без мастера и валидаторов. Δ от нейтрали
**в натуральных единицах**. Журнал исключений, именованные когорты. → работающий продукт,
который собирает и показывает измерения, но **суждений о значимости не выносит** —
до Этапа 1.5 их выносить не на чем.

**Этап 1.5 (Р-20, Р-35).** Режим повторяемости: протокол test-retest на скелете Этапа 1,
расчёт ICC, SEM и собственного SDC, разброс между операторами, паспорт параметра,
выпуск `sdc_<version>.yaml` с `source: own_repeatability`. **С этого момента появляется
`RI`** и любые суждения о значимости. Это же слой 1 аналитики (§9.8а) — он обязателен,
потому что остальные слои без него не имеют опоры. Внешнего источника SDC для этой комбинации оборудования не существует.

Порядок именно такой (Р-32): скелет данных нужен режиму повторяемости, режим
повторяемости нужен `RI`, а машинерия протокола — Этап 2 — нужна только после того,
как есть чем мерить значимость.

**Этап 2 — машинерия протокола.** Мастер сессии, **план сессии и отклонения от плана**,
валидаторы по матрице §8.1, реестр проб, sham-условие, рандомизация с seed, проходы
и подтверждающий повтор, слепой разбор качества, эффекты и пороги, ранжирование,
карта проб, история с правилом сопоставимости. Предрегистрация гипотез, lockbox,
контроль множественности.

**Этап 3.** Bridge Agent. Pedoscan и нижний контур: компенсация длины ног, домен опоры,
комбинированная проба, отпечатки и карта разности.

**Этап 4.** ЭМГ: контейнер сигнала, адаптер под конкретный миограф, уровень E, метрики,
флаги качества, мышечный индекс.

**Этап 5.** Кондилография: ручной ввод, адаптеры, связка по коду пробы, когерентность,
карта отклика.

**Этап 6.** Движок правил, библиотека упражнений, стельки, прогноз, режим тренера —
только по параметрам с установленным направлением и подтверждёнными закономерностями
(Р-19). Доступен при `intended_use = clinical`.

**Этап 7.** Когортный дашборд, обезличенный экспорт, база исходов, калибровка прогноза
на собственных данных. Слои 3 и 4 аналитики (§9.8а): многомерный отклик, оси отклика,
иерархические модели, типология отзывчивости, калькулятор мощности.

MUST (Р-35): **слой 2 аналитики — аналитика протокола — входит в Этап 2**, а не в 7.
Калибровка `settle_sec` и `carryover_min` по собственным данным сокращает длительность
сессии, а значит и стоимость набора; отложенная на конец, она не успевает повлиять
на протокол, ради разработки которого платформа и строится. Снапшот когорты —
Этап 2 по той же причине: без него слой 3 невоспроизводим.

Порядок 1 → 1.5 → 2 фиксирован. Этапы 3–5 могут идти параллельно разными исполнителями,
поскольку опираются на общий канонический слой.

MUST (Р-14, Р-15, Р-16, Р-17): исследовательский минимум входит в Этапы 1 и 2, а не
откладывается на Этап 7. Данные, собранные без предрегистрации, журнала исключений,
фиксации рандомизации и слепой оценки качества, задним числом доказательными не станут —
они останутся пригодны только для поискового анализа. Подробности —
[исследовательский контур](./research-framework.md#12-порядок-этапов).

---

## 18. Явно вне рамок

Автоматическое проектирование конструкции шины или стельки; постановка диагноза;
интерпретация причинности между уровнями; реальный биофидбек; замена решения врача.
Модуль измеряет, сопоставляет и предлагает — решение остаётся за человеком.
