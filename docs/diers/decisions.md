# Решения по ТЗ

Журнал решений по открытым вопросам к
[ТЗ «Постуральный модуль DIERS v1.0»](./TZ-postural-module-v1.0.md).

Разделы 4–12 ТЗ — контракт данных и алгоритмов, меняются только через ревью (§0).
Каждое изменение этих разделов фиксируется здесь: вопрос, решение, что именно
поправлено в тексте ТЗ, какие следствия за собой тянет.

Открытых вопросов не осталось: Р-1…Р-9 закрывают В-1…В-9, Р-10…Р-12 — конфликты К-1…К-3
из [сопоставления](./gap-analysis.md). Р-13…Р-20 приняты после уточнения, что платформа —
научно-практическая: она выявляет закономерности, а не применяет готовые; развёрнуто
в [исследовательском контуре](./research-framework.md). Р-21…Р-27 переводят документ
на правовое поле РФ; развёрнуто в [нормативной рамке](./regulatory-rf.md). Р-28…Р-34
устраняют противоречия, найденные сквозным разбором; находки и предложения по аналитике —
в [разборе логики](./review-consistency.md).

Часть решений расширяет ТЗ сверх заданного вопроса — новые значения, поля, эндпоинты.
Такие места помечены **⚠ расширение** и подлежат отдельному подтверждению: их можно
отменить, не трогая остальное.

| # | Вопрос | Суть решения |
|---|---|---|
| [Р-1](#р-1--состояние-сессии--одно-поле) | В-1 | одно поле состояния, домен — машина §8.1 |
| [Р-2](#р-2--переанализ-при-смене-версий) | В-2 | analyses только на вставку, переанализ штатной операцией |
| [Р-3](#р-3--пять-версий-конфигурации) | В-3 | пять версий вместо трёх: + profile, + registry |
| [Р-4](#р-4--порядок-вердиктов-комбинированной-пробы) | В-4 | явный приоритет, добавлены synergistic и indeterminate |
| [Р-5](#р-5--два-списка-вместо-ранжирования-по-модулю) | В-5 | shortlist только улучшающие, ухудшающие — отдельным списком |
| [Р-6](#р-6--переименование-conflict-в-когерентности) | В-6 | §9.5 `conflict` → `posture_muscle_conflict` |
| [Р-7](#р-7--эндпоинты-переходов-состояния) | В-7 | 3 новых эндпоинта, разделение явных и неявных переходов |
| [Р-8](#р-8--историзм-neutral_definition) | В-8 | копия в `sessions`, подтверждено |
| [Р-9](#р-9--требования-к-patient_ref) | В-9 | непроизводен от ПДн и неперечислим, таблица соответствия у хоста |
| [Р-10](#р-10--язык-домена-и-граница-с-хостом) | К-1 | отдельный Python-сервис, TS-модуль целевым не становится |
| [Р-11](#р-11--владение-данными-пациента) | К-2 | пациент не пишет в модуль |
| [Р-12](#р-12--что-такое-myoline) | К-3 | myoline = сила; legacy-данные трактуются как ЭМГ |
| [Р-13](#р-13--индекс-отзывчивости-ri-работа-без-норм) | контекст | `RI` рядом с `PI`; отсутствие норм ничего не блокирует |
| [Р-14](#р-14--подпороговые-значения-хранятся-и-анализируются) | контекст | подпороговое не интерпретируется клинически, но анализируется на когорте |
| [Р-15](#р-15--sham-условие) | контекст | sham обязателен в исследовательских протоколах |
| [Р-16](#р-16--рандомизация-и-эффект-порядка) | контекст | схема и seed фиксируются, перенос эффекта проверяем |
| [Р-17](#р-17--слепой-разбор-качества) | контекст | код пробы скрыт до фиксации решений; исключения обратимы |
| [Р-18](#р-18--направление-unknown) | контекст | `direction = unknown` по умолчанию для нового параметра |
| [Р-19](#р-19--intended_use-и-условие-загрузки-правил) | контекст | `research`/`clinical`; правило без подтверждения не загружается |
| [Р-20](#р-20--собственные-нормы-и-пороги-этап-15) | контекст | платформа порождает SDC и нормы из своих данных |
| [Р-21](#р-21--правовая-рамка-рф-вместо-gdpr-и-mdr) | право РФ | ФЗ-152 и ФЗ-323 вместо GDPR; ЕАЭС вместо MDR |
| [Р-22](#р-22--подпись-отчёта-укэп) | право РФ | подпись — УКЭП документа, не нажатие кнопки |
| [Р-23](#р-23--локализация-и-замкнутый-контур-обработки) | право РФ | локализация обязательна; LLM и телеметрия — в контуре |
| [Р-24](#р-24--согласия-идс-исследование-передача-тренеру) | право РФ | три раздельных согласия, отзыв через уничтожение соответствия |
| [Р-25](#р-25--статус-медизделия-и-класс-риска) | право РФ | `research` не медизделие; `clinical` требует РУ |
| [Р-26](#р-26--этическая-экспертиза-и-гост-р-исо-14155) | право РФ | одобрение ЛЭК до первого участника |
| [Р-27](#р-27--стандарты-жизненного-цикла-в-приёмку) | право РФ | 62304, 14971, 13485 с Этапа 1, не при регистрации |
| [Р-28](#р-28--проходы-сессии-и-цикл-probes_running--imported) | П-1, П-5 | проходы сессии; подтверждающий повтор — второй проход |
| [Р-29](#р-29--план-сессии-и-отклонения-от-плана) | П-3, П-6, П-7 | план сессии как сущность, отклонения от плана |
| [Р-30](#р-30--дрейф-нейтрали-в-единицах-sdc) | П-2, П-10 | дрейф в единицах SDC, флаг по остатку после поправки |
| [Р-31](#р-31--матрица-валидатор--переход) | П-3 | каждый валидатор привязан к конкретному переходу |
| [Р-32](#р-32--скелет-данных-в-этап-1) | П-4 | скелет данных в Этап 1, машинерия протокола в Этап 2 |
| [Р-33](#р-33--мелкие-согласования) | П-8, П-11…П-13 | роли↔варианты, повторный отчёт, LLD, режим research |
| [Р-34](#р-34--автопереанализ-и-хранение-analyses) | П-9 | автопереанализ при публикации конфигурации, вытеснение |
| [Р-35](#р-35--аналитический-слой-в-тз) | аналитика | четыре слоя аналитики внесены в ТЗ; слои 1 и 2 — в Этапы 1.5 и 2 |
| [Р-36](#р-36--реальные-протоколы-приборов-реестр-20262) | реальные данные | реестр 2026.2: единицы исправлены, добавлены динамика и кондилография |
| [Р-37](#р-37--стыковка-модальностей-и-классификация-проб) | стыковка | ЭМГ и myoline; автоподгрузка; три сигнала на пробу и раскладка проб |
| [Р-38](#р-38--три-раскладки-и-обе-темы-1461) | §14.6 | телефон, планшет, компьютер; тема доступна везде |
| [Р-39](#р-39--обзор-от-сустава-до-стоп-и-сравнение-набора-проб) | §14.3 | анатомическая раскладка вне версий конфигурации; таблица на N проб |

---

## Р-1 · Состояние сессии — одно поле

**Дата:** 2026-08-16
**Вопрос:** В-1 — §4 задавал `sessions.status ∈ {draft, ready, analyzed, reported}`,
§8.1 — машину `prepared → … → analyzed`. Два словаря на одну сущность.

**Решение:** одно поле. Домен `sessions.status` — машина состояний §8.1;
прежний набор `draft|ready|analyzed|reported` упразднён.

**Итоговый домен (8 значений):**

```
prepared → baseline_recorded → probes_running → imported
  → quality_reviewed → shortlist_confirmed → analyzed → reported
```

Позднее домен дополнен состоянием `plan_approved` между `prepared` и `baseline_recorded`
([Р-29](#р-29--план-сессии-и-отклонения-от-плана)) — итого 9 значений.

`reported` перенесён из старого домена как терминальное состояние — §8.1
заканчивалась на `analyzed`, но выпуск отчёта состоянием сессии остаётся.

**Соответствие старых значений новым:**

| Было (§4) | Стало |
|---|---|
| `draft` | `prepared` |
| `ready` | `shortlist_confirmed` |
| `analyzed` | `analyzed` |
| `reported` | `reported` |

**Правки в ТЗ:**
- §4, таблица `sessions`: домен `status` заменён на машину §8.1, поле объявлено `not null`;
- §4: добавлено поле `low_confidence boolean default false`;
- §8.1: машина дополнена терминальным `→ reported`, зафиксированы односторонность
  переходов, запись в `audit_log` и разграничение с `reports.status`.

**Следствия:**

1. **`low_confidence` — отдельное поле, не значение статуса.** §8.2 требует помечать
   сессию при дрейфе нейтрали выше порога. Признак ортогонален стадии: помеченная
   сессия продолжает движение по машине до `reported`. Значением статуса он быть
   не может, поэтому вынесен в отдельную колонку. *Это добавление к схеме — если
   предполагалось иное место хранения, скажите.*

2. **Снята перегрузка слова `draft`.** До решения `draft` означал одновременно раннюю
   стадию сессии (§4) и неподписанный вывод (§1). Теперь `draft`/`signed` — только
   про отчёт, в `reports.status`; у сессии такого значения нет.

3. **`prepared` — начальное значение при вставке.** Поле `not null`, значения по
   умолчанию на уровне БД ТЗ не задаёт; `POST /sessions` (§13) создаёт сессию
   сразу в `prepared`.

4. **Переходы односторонние.** Возврат назад по машине схемой не предусмотрен;
   если понадобится (например, доснять пробу после `imported`), это отдельное
   решение.
   → Понадобилось: [Р-28](#р-28--проходы-сессии-и-цикл-probes_running--imported) разрешает
   цикл `probes_running ⇄ imported` для подтверждающего повтора. Прочие переходы
   остаются односторонними.

**Открыто после решения:** нужен ли явный переход `analyzed → reported` в API.
→ Закрыто в [Р-7](#р-7--эндпоинты-переходов-состояния): добавлен
`POST /analyses/{id}/report`.

---

## Р-2 · Переанализ при смене версий

**Дата:** 2026-08-16
**Вопрос:** В-2 — §12 требует совпадения версий для сопоставимости, значит любое
обновление норм разрывает историю; сырые данные сохраняются, но эндпоинта переанализа
в §13 нет.

**Решение:** переанализ — штатная операция, а не аварийное восстановление.

1. `analyses` — только на вставку. Переанализ создаёт **новую** строку; прежние
   сохраняются навсегда (инвариант §4.5).
2. `POST /sessions/{id}/analyze` принимает необязательный набор версий. Без него —
   активная конфигурация, с ним — считает по указанной.
3. Добавлен `POST /patients/{ref}/reanalyze {versions, session_ids?}` — пересчёт курса
   под общий набор версий, асинхронно через очередь (§3), возвращает `job_id`.
4. **Сверка сопоставимости идёт по `analyses`, а не по `sessions`.** Версии в `sessions` —
   что действовало при обследовании; версии в `analyses` — с чем посчитали. Без этого
   разделения переанализ бессмысленен: сравнивались бы исторические версии, а не
   фактически применённые.
5. `GET /patients/{ref}/history` по умолчанию берёт **самый свежий набор версий,
   доступный для всех сессий пациента**, и печатает его рядом с графиком. Это
   превращает §12 из ограничения в рабочее правило: история остаётся связной, пока
   для сессий существует общий пересчёт.

**Подписанные отчёты не затрагиваются.** Отчёт привязан к своей строке `analyses`
(§4). Переанализ не отменяет и не переписывает подписанное заключение — это требование
воспроизводимости §1/§15, а не деталь реализации.

**Правки в ТЗ:** §4 (комментарий к `analyses`, инвариант 5), §12 (сверка по `analyses`,
подбор набора версий), §13 (`versions` у analyze, `reanalyze`, `?versions=` у history).

⚠ **расширение:** эндпоинт `POST /patients/{ref}/reanalyze` и параметр `?versions=`
в истории — сверх исходного §13.

---

## Р-3 · Пять версий конфигурации

**Дата:** 2026-08-16
**Вопрос:** В-3 — инвариант §4.2 перечисляет три версии, но `W_domain` из «профиля
клиники» (§9.2) в них не входит, значит смена весов меняет `result` при неизменном
`input_hash`.

**Решение:** пять версий вместо трёх. Добавлены `profile_version` и `registry_version`.

| Версия | Что покрывает | В `input_hash` | В сопоставимости §12 |
|---|---|---|---|
| `norms_version` | нормы (mean, sd, страты) | да | да |
| `thresholds_version` | SDC, `thresholds.PI` | да | да |
| `profile_version` | `W_domain`, `w_p`, `z_cap`, границы категорий z | да | да |
| `registry_version` | `param_registry`: домен, единица, направление, участие в PI | да | да |
| `rules_version` | правила рекомендаций | да | **нет** |
| `protocol_version` | реестр проб: `settle_sec`, `carryover_min` | нет | нет |

Обоснования разнесения:

- **`registry_version` понадобился из того же соображения, что и профиль.** §7 задаёт
  реестр параметров, но версии ему не давал. Смена домена параметра, его направления
  или участия в PI меняет результат при тех же измерениях — ровно та же дыра в
  детерминизме, про которую вопрос.
- **`rules_version` не входит в сопоставимость.** Правила влияют на рекомендации, а не
  на измеренные величины; иначе обновление библиотеки упражнений рвало бы историю
  постуральных показателей без всякой причины.
- **`protocol_version` не входит в `input_hash`.** Реестр проб определяет, как
  обследование проводится, а не как считается: на арифметику по уже собранным
  каноническим данным он не влияет. Поле в `sessions` уже было.

**Правки в ТЗ:** §4 (`sessions`, `analyses`, инвариант 2), §7 (версионирование реестра),
§10 (файлы `profiles/` и `registry/`), §12 (состав проверки).

⚠ **расширение:** `registry_version` — сверх заданного вопроса; вопрос был про веса.

---

## Р-4 · Порядок вердиктов комбинированной пробы

**Дата:** 2026-08-16
**Вопрос:** В-4 — `additive` и `dominant` могут выполниться одновременно, приоритет
в тексте задан неявно.

**Решение:** порядок проверки объявлен нормативным, первое совпадение выигрывает:

```
conflict → synergistic → dominant → additive → indeterminate
```

- **`conflict` первым.** Комбинация хуже лучшего одиночного уровня остаётся конфликтом,
  даже если численно совпала с суммой. Это безопасная сторона: §11 MUST запрещает при
  конфликте назначать оба уровня.
- **`dominant` раньше `additive`.** Пересечение возникает, когда вклад одного уровня
  в пределах шума. Честная формулировка для такого случая — «работает один уровень»,
  а не «вклады сложились»: вторая подсказывает врачу назначить оба.
- **`synergistic`** — комбинация лучше суммы одиночных, `ΔPI(AB) < add − thr`.
  В исходном тексте случай не назван, а он клинически самый интересный.
- **`indeterminate`** — не совпало ничего. Не интерпретируется, в рекомендации не идёт,
  требует повтора пробы. Без этого значения движку правил пришлось бы гадать.

**Правки в ТЗ:** §9.7 полностью переписан.

⚠ **расширение:** два новых значения вердикта — `synergistic`, `indeterminate`.

---

## Р-5 · Два списка вместо ранжирования по модулю

**Дата:** 2026-08-16
**Вопрос:** В-5 — топ-2 по `|ΔPI|` ставит ухудшающую пробу в конкуренцию с улучшающей.

**Решение:** ранжирование по модулю отменено, вводятся два раздельных списка.

- **`shortlist`** — только достоверные улучшения (`ΔPI < −thresholds.PI`), сортировка
  по величине улучшения. Подтверждающий повтор (§8.3) применяется к топ-2 из этого списка.
- **`notable_worsening`** — только достоверные ухудшения (`ΔPI > +thresholds.PI`).
  Печатаются в отчёте и в режиме тренера (§14.5 прямо требует показывать конфликты —
  они меняют выбор упражнений), но в рекомендации не попадают никогда и подтверждающим
  повтором не проверяются, помечаются `unconfirmed`.

Ухудшение диагностически ценно, но кандидатом в терапию не является — смешивать их
в одном ранжированном списке нельзя. Повтор ухудшающих проб не делается сознательно:
это удваивает длительность сессии ради данных, которые всё равно не станут назначением.

Пробы внутри полосы шума не попадают ни в один список — прямое следствие §9.3.

**Правки в ТЗ:** §8.3 переписан.

⚠ **расширение:** список `notable_worsening` как отдельная сущность результата.

---

## Р-6 · Переименование `conflict` в когерентности

**Дата:** 2026-08-16
**Вопрос:** В-6 — слово `conflict` занято дважды: когерентность §9.5 и вердикт
комбинированной пробы §9.7.

**Решение:** переименовано значение в §9.5: `conflict` → `posture_muscle_conflict`.
Вердикт §9.7 сохраняет имя.

Переименован именно §9.5, потому что §9.7 `conflict` упомянут в MUST-правиле §11
(«при `conflict` движок правил не выдаёт одновременное назначение обоих уровней») —
трогать формулировку, на которую ссылается нормативное требование, дороже, чем
переименовать значение отображения.

**Правки в ТЗ:** §9.5, таблица когерентности.

---

## Р-7 · Эндпоинты переходов состояния

**Дата:** 2026-08-16
**Вопрос:** В-7 — §8.1 делает `quality_reviewed` и `shortlist_confirmed` обязательными
шагами с решением врача, но эндпоинтов для них в §13 нет.

**Решение:** переходы разделены на два типа.

**Неявные** — срабатывают по факту появления данных, отдельного вызова не требуют:

| Переход | Триггер |
|---|---|
| `prepared → baseline_recorded` | импортирована первая проба-нейтраль |
| `→ probes_running` | импортирована первая не-базовая проба |
| `→ imported` | очередь разбора пуста, нераспознанных файлов нет |

**Явные** — требуют решения человека, каждый пишется в `audit_log`:

```
POST /sessions/{id}/quality-review   {decisions: [{measurement_id, verdict, reason}]}
POST /sessions/{id}/shortlist        {confirmed_probe_codes[]}
POST /sessions/{id}/analyze          {versions?}
POST /analyses/{id}/report           {variant}
```

Последний закрывает вопрос, оставшийся открытым после Р-1: `analyzed → reported`
делает выпуск отчёта. `POST /analyses/{id}/sign` относится к отчёту (`reports.status`)
и состояние сессии не меняет.

**Правки в ТЗ:** §13 (три новых эндпоинта, разделение типов переходов).

⚠ **расширение:** три эндпоинта и разделение переходов на явные и неявные.

---

## Р-8 · Историзм `neutral_definition`

**Дата:** 2026-08-16
**Вопрос:** В-8 — поле лежит в `patients_ref` одним значением, а §8.1.1 допускает смену;
чтобы прошлые визиты сохранили своё определение нейтрали, значение нужно копировать
в `sessions`.

**Решение:** подтверждено. `sessions.neutral_definition not null` — копия значения на
момент создания сессии, по той же логике, что и версии конфигурации (инвариант §4.3).
`patients_ref.neutral_definition` остаётся текущим значением по умолчанию для новых
сессий. Смена у пациента прошлые сессии не переписывает и фиксируется в `audit_log`.
Сверка сопоставимости §12 читает копию из сессии.

**Правки в ТЗ:** §4 (`sessions`, инвариант 6), §12.

---

## Р-9 · Требования к `patient_ref`

**Дата:** 2026-08-16
**Вопрос:** В-9 — кто владеет соответствием `patient_ref ↔ учётная запись` и где ключ,
если §15 требует держать его вне модуля.

**Решение:**

1. Таблицу соответствия ведёт **хост**; модуль её не хранит и за identity к хосту
   не обращается.
2. `patient_ref` **непроизводен от ПДн** и **неперечислим**: UUIDv4 либо HMAC от
   идентификатора хоста с ключом, хранящимся у хоста.
3. Последовательные идентификаторы хост-системы в этой роли запрещены. Псевдоним вида
   `user_id = 42` не является обезличиванием: он перечислим и тривиально сопоставим
   с профилем в хост-системе, из-за чего требование §15 об обезличенном экспорте
   когорты перестаёт выполняться.

**Правки в ТЗ:** §13 (MUST к `patient_ref`), §15 (ссылка на требования).

---

## Р-10 · Язык домена и граница с хостом

**Дата:** 2026-08-16
**Вопрос:** К-1 — §3 запрещает смешивать домен между языками и требует Python-сервис,
если хост на другом языке; хост `fitmed-ecosystem` написан на TypeScript.

**Решение:** применяется предусмотренный §3 вариант. Модуль — отдельный Python-сервис
со своей БД, разворачивается самостоятельно, хост обращается к нему только по §13.
Существующий TS-модуль DIERS в fitmed целевым не становится и в кодовую базу модуля
не переносится.

Альтернатива — переписать §3 под TypeScript — отвергнута: §16 требует property-тестов
знаковых соглашений, табличных тестов алгоритмов и обработки сигнала ЭМГ
(фильтрация, спектральная интерполяция, MDF), а §3 предполагает аналитические ноутбуки
на тех же моделях данных. Ради переиспользования модуля, который по Р-11 всё равно
подлежит замене, менять стек не стоит.

**Правки в ТЗ:** §3, уточнение после правила о выносе домена.

---

## Р-11 · Владение данными пациента

**Дата:** 2026-08-16
**Вопрос:** К-2 — в существующей системе исследование принадлежит учётной записи
пациента: он сам создаёт, редактирует и удаляет свои данные. ТЗ строит клинический
инструмент.

**Решение:** пациент не является субъектом записи в модуль. Данные вводит `operator`,
выводы подписывает `clinician`. Пациент получает результат только пациентским вариантом
подписанного отчёта (`variant=patient`) и не имеет ни записи, ни удаления. Роль `coach` —
чтение своего варианта отчёта без доступа к сырым величинам (§14.5).

Удаление пациентом несовместимо с аудитом (§15) и воспроизводимостью (§4): подписанное
заключение обязано оставаться проверяемым, а исходные данные под ним — существовать.

**Последствие за пределами модуля:** сценарий «пациент ведёт свои исследования сам»,
работающий в fitmed, в целевой системе не воспроизводится. Это продуктовое изменение,
а не техническое, — если оно нежелательно, решение нужно пересмотреть до Этапа 1.

**Правки в ТЗ:** §15 (MUST о ролях записи).

⚠ **расширение:** решение меняет продуктовую модель, а не только схему прав.

---

## Р-12 · Что такое myoline

**Дата:** 2026-08-16
**Вопрос:** К-3 — в §9.8 myoline это изометрическая сила, не участвующая в анализе проб;
в существующей системе таблица `myoline_studies` хранит ЭМГ в мкВ.

**Решение:** термин закрепляется за изометрической силой, как в ТЗ. ЭМГ — отдельная
модальность со своими таблицами (§4). Унаследованные данные `myoline_studies` по
существу являются ЭМГ и при любой миграции трактуются как ЭМГ, а не как myoline.

ТЗ не меняется по существу — меняется трактовка legacy-данных. Обратный вариант
(признать myoline синонимом ЭМГ) отвергнут: он сломал бы MUST §9.8 о неучастии myoline
в анализе проб, а это содержательное методическое требование, а не вопрос именования.

**Правки в ТЗ:** §9.8, примечание о трактовке legacy-данных.


---

## Р-13 · Индекс отзывчивости RI, работа без норм

**Дата:** 2026-08-16
**Контекст:** платформа исследовательская; опубликованных норм для этой комбинации
оборудования и стратификации нет, а имеющиеся получены на других популяциях и другой
аппаратуре — принять их значит встроить в результат чужое допущение.

**Проблема:** в исходном ТЗ зависимость жёсткая — `z` требует норм, `PI` требует `z`,
`ΔPI` требует `PI`, ранжирование и шорт-лист требуют `ΔPI`. Нет норм — не работает ничего.

**Решение:** два независимых индекса. `PI` — популяционный, требует норм. `RI` —
отзывчивость, опирается на собственную нейтраль пациента и требует только SDC:

```
RI_domain = Σ_p w_p · min(|Δ_p| / SDC_p, cap)
RI        = Σ_domain W_domain · RI_domain
```

`RI` — основной индекс исследовательского контура. Отсутствие норм не блокирует ни один
сценарий: параметр без норм входит в `RI` и не входит в `PI`, колонка нормы — прочерк.

Это не смягчение требований, а более точная опора: метод по построению измеряет отклик
на пробу, а не удалённость от популяции, поэтому норма популяции не должна быть входом
для оценки отклика.

**Правки в ТЗ:** §2 (глоссарий), §9.2 (переименован в «Индексы: PI и RI»), §16 (тест
работы без норм).

---

## Р-14 · Подпороговые значения хранятся и анализируются

**Дата:** 2026-08-16
**Контекст:** задача — выявить закономерности, ранее отвергавшиеся.

**Проблема:** §9.3 MUST исключает значения ниже SDC из ранжирования и интерпретации.
Для клинического вывода это верно. Применённое буквально ко всей платформе, это правило
закрывает доступ ровно к той зоне, где искомое и находится: закономерности, отвергавшиеся
как «в пределах погрешности».

**Решение:** правило разделяется по типу вывода, а не по данным.

| | клинический контур | исследовательский контур |
|---|---|---|
| Подпороговое | не интерпретируется, нейтральный цвет | анализируется, входит в модели |
| Единица вывода | этот пациент | когорта / серия n-of-1 |
| Может стать назначением | да, после подписи врача | никогда напрямую |

SDC — порог различимости **однократного измерения у одного пациента**. Ошибка групповой
оценки меньше индивидуальной в √n раз, поэтому систематический сдвиг, неразличимый
в отдельном обследовании, обнаруживается на серии. Отбрасывать подпороговые значения
на входе запрещено.

Тем же решением снимается запрет §9.6 на аппроксимацию поверхностью: он остаётся для
клинического отображения, в исследовательском контуре моделирование ожидаемо, но его
результат — гипотеза со статусом `exploratory`.

**Правки в ТЗ:** §9.3, §9.6.

---

## Р-15 · Sham-условие

**Дата:** 2026-08-16
**Решение:** в исследовательских протоколах обязательна sham-проба — условие,
неотличимое для пациента от активного, но методически неактивное. Код `CTRL_SHAM_<уровень>`,
`role = control`. Валидатор §8.1.9 блокирует сессию со `study_mode = true` и
терапевтической пробой без sham соответствующего уровня.

**Обоснование:** повторная нейтраль контролирует дрейф во времени, но не сам факт
вмешательства. Без sham изменение позы под пробой объясняется временем, вниманием,
утомлением и ожиданием эффекта. Разница «активная проба vs sham» переживёт внешнюю
критику, разница «активная проба vs ничего» — нет.

**Правки в ТЗ:** §2 (коды проб), §8.1 (валидатор 9).

---

## Р-16 · Рандомизация и эффект порядка

**Дата:** 2026-08-16
**Проблема:** `carryover_min` (§8.1.4) — непроверенное допущение, что за указанное время
эффект предыдущей пробы исчез. Для нового протокола само это допущение подлежит проверке.

**Решение:** в `sessions` фиксируются `randomization_scheme`
(`none` | `random_within_group` | `counterbalanced`) и `randomization_seed`;
последовательность воспроизводима из seed, фактический порядок уже есть в `trials.ordinal`.
Валидатор §8.1.10 требует seed при любой схеме, кроме `none`.

Это превращает перенос эффекта и эффект порядка из допущения в проверяемую гипотезу:
позиция пробы в последовательности становится ковариатой, а `carryover_min` калибруется
по собственным данным.

**Правки в ТЗ:** §4 (`sessions`), §8.1 (валидатор 10).

---

## Р-17 · Слепой разбор качества

**Дата:** 2026-08-16
**Проблема:** шаг `quality_reviewed` требует решения человека по каждой строке. Если
оценивающий видит код пробы, он знает, какая проба «должна» сработать, и исключение
артефактов становится необъявленной степенью свободы исследователя — классическое
основание отвергнуть результат.

**Решение:**

1. Экран разбора скрывает код пробы, роль, порядковый номер и результаты анализа.
   Видны сигнал и флаги качества. Раскрытие — после фиксации всех решений по сессии.
2. Каждое исключение пишется в журнал (`exclusions`) с кодом причины, автором, отметкой
   о слепоте и **обратимо**: анализ обязан уметь пересчитаться на полном наборе для
   проверки чувствительности результата к исключениям.
3. Набор исключений входит в `input_hash` когортного анализа.

Необратимое исключение равносильно потере данных, поэтому физическое удаление
измерений запрещено.

**Правки в ТЗ:** §8.1 (валидатор 11), §16 (тесты слепоты и обратимости).

---

## Р-18 · Направление `unknown`

**Дата:** 2026-08-16
**Проблема:** `param_registry.direction` кодирует клиническое допущение о том, что
считать ухудшением. Для параметра, по которому закономерность только предстоит
установить, это предрешение результата.

**Решение:** домен расширен значением `unknown`, и оно — **значение по умолчанию для
нового параметра**. Параметр с `unknown` входит в `RI` по модулю, показывается
нейтральным цветом без слов «улучшение»/«ухудшение», в правила рекомендаций не входит,
в отчёте помечается как параметр под изучением.

Смена `unknown` на определённое направление — изменение `registry_version` со всеми
последствиями для сопоставимости, и требует ссылки на подтверждающий анализ, который
это направление установил. Это и есть механизм, которым результат исследования
попадает в продукт: не правкой конфигурации, а через зарегистрированное подтверждение.

**Правки в ТЗ:** §7.

---

## Р-19 · `intended_use` и условие загрузки правил

**Дата:** 2026-08-16
**Контекст:** платформа не опирается на готовые решения и рекомендации в лечении.

**Решение:** развёртывание имеет явный режим `intended_use ∈ {research, clinical}`.

- **`research`** — движок правил (§11) отключён. Платформа выдаёт наблюдения, отклики
  и статистику; отчёт содержит измеренное и найденное, не предписанное.
- **`clinical`** — движок работает, но правило, опирающееся на параметр с
  `direction = unknown` либо на неподтверждённую закономерность, **не загружается**:
  валидация конфигурации при старте отклоняет такой файл.

Правило нельзя написать раньше, чем закономерность подтверждена, — технически,
а не по регламенту.

Режим `research` не выводит платформу из-под §15 (аудит, версионирование,
воспроизводимость): для исследования они нужны не меньше. Но по праву РФ отсутствие
терапевтического вывода означает, что платформа не является медицинским изделием —
см. [Р-25](#р-25--статус-медизделия-и-класс-риска). Границу между режимами следует
зафиксировать письменно до пилота.

**Правки в ТЗ:** §1, §11, §17 (Этап 6).

---

## Р-20 · Собственные нормы и пороги, Этап 1.5

**Дата:** 2026-08-16
**Проблема:** в исходном ТЗ нормы и SDC — внешний вход с обязательным `source`.
Для новой комбинации оборудования и собственных протоколов внешнего источника
не существует, из-за чего Б-1 (SDC-исследование) и Б-2 (нормы) стояли внешними
блокерами с неопределённым сроком.

**Решение:** платформа **порождает** нормы и пороги из собственных данных.

- **Режим повторяемости** — протокол test-retest с переустановкой пациента; по парам
  считается SDC каждого параметра. Выпуск `thresholds/sdc_<version>.yaml` с
  `source: own_repeatability`, ссылкой на когорту, числом пар и доверительным интервалом.
- **Собственные нормы** — `source: own_cohort` со стратификацией и числом наблюдений.
  Статус тот же, что у литературных; отличие только в поле `source`.
- До появления собственного SDC действует `source: demo` с предупреждением на всех
  экранах — уже требование §10.

**Следствие для плана:** добавлен **Этап 1.5** между 1 и 2. Это не задержка, а перенос:
SDC-исследование было внешним блокером Этапа 2, теперь это функция платформы с понятным
содержанием и измеримым результатом.

**Правки в ТЗ:** §10, §17 (Этап 1, новый Этап 1.5, порядок 1 → 1.5 → 2).


---

## Р-21 · Правовая рамка РФ вместо GDPR и MDR

**Дата:** 2026-08-16
**Контекст:** платформа разрабатывается и эксплуатируется в РФ. Исходный текст ТЗ
ссылался на GDPR (§15) и MDR (§1) — европейские режимы, здесь неприменимые.

**Решение:** правовая рамка — законодательство РФ и право ЕАЭС.

| Было | Стало |
|---|---|
| «данные особой категории по GDPR» | специальная категория ПДн, ФЗ-152 ст. 10 |
| DPIA | модель угроз + уровень защищённости (ПП РФ № 1119) + меры (Приказ ФСТЭК № 21) + оценка вреда субъектам (ФЗ-152 ст. 18.1) |
| «правовое основание до пилота» | уведомление РКН об обработке (ст. 22), назначение ответственного за организацию обработки |
| MDR | ФЗ-323 ст. 38; ПП РФ № 1416; Решение Совета ЕЭК № 46 |
| — | врачебная тайна, ФЗ-323 ст. 13 |
| — | уведомление об инциденте: 24 ч на факт, 72 ч на результаты расследования (ФЗ-152 ст. 21) |

**Следствие для аудита.** Срок в 24 часа выполним, только если журнал фиксирует
не факт экспорта, а **состав выгрузки** — перечень `patient_ref` и сессий. Иначе
определить круг затронутых субъектов за сутки невозможно. Это ужесточение §15.

**Правки в ТЗ:** §1, §15. Развёрнуто в [нормативной рамке](./regulatory-rf.md).

---

## Р-22 · Подпись отчёта: УКЭП

**Дата:** 2026-08-16
**Проблема:** §1 требовал перехода в `signed` «действием пользователя с ролью врач».
В правовом поле РФ медицинский документ в электронной форме подписывается усиленной
квалифицированной электронной подписью медработника (ФЗ-63; Приказ МЗ РФ № 947н).
Нажатие кнопки таким документом отчёт не делает.

**Решение:** контракт `POST /analyses/{id}/sign` меняется:

```
{ clinician_ref, signature, signed_document_hash, timestamp_token? }
```

- подписывается **хэш сформированного документа**, а не запись в БД;
- подпись хранится с документом, проверяется при каждом открытии, результат проверки
  отображается: кем, когда, был ли сертификат действителен на момент подписания;
- метка доверенного времени обязательна — иначе истечение срока сертификата
  обесценивает подпись задним числом;
- `reports.pdf_ref` неизменяем после подписания; исправление выпускается **новым**
  документом со ссылкой на исправляемый, прежний не удаляется.

Это же согласуется с инвариантом §4.5 (`analyses` только на вставку): подписанное
заключение остаётся проверяемым независимо от последующих переанализов.

**Правки в ТЗ:** §1, §13, §16.

⚠ **расширение:** метка доверенного времени и требование неизменяемости документа —
сверх буквального требования норм, но без них подпись не выдержит проверки через годы.

---

## Р-23 · Локализация и замкнутый контур обработки

**Дата:** 2026-08-16
**Проблема:** §15 объявлял on-prem «основным сценарием», допуская альтернативы.
§11 разрешал LLM для формулировки текста отчёта без ограничений по размещению.

**Решение:**

1. **Локализация обязательна.** ФЗ-152 ст. 18 ч. 5 требует, чтобы базы данных с ПДн
   граждан РФ находились на территории РФ. On-prem либо российский хостинг — не
   предпочтение, а единственный допустимый вариант.
2. **Контур обработки замкнут.** Ни один сценарий не обращается к внешним хостам:
   ни LLM, ни телеметрия, ни сбор ошибок, ни аналитика использования.
3. **LLM разворачивается в контуре организации**, в промпт не попадают идентификаторы,
   `patient_ref`, даты обследований и любые комбинации значений, позволяющие соотнести
   текст с лицом. При недоступности модели — шаблонные формулировки, что уже
   требование §11.

Отправка фрагмента заключения во внешний сервис за пределами РФ — трансграничная
передача специальной категории ПДн (ФЗ-152 ст. 12) со всеми последствиями. Это самый
недооценённый риск исходного текста: строка про LLM выглядела деталью реализации.

**Правки в ТЗ:** §11, §15, §16 (тест замкнутости контура).

---

## Р-24 · Согласия: ИДС, исследование, передача тренеру

**Дата:** 2026-08-16
**Проблема:** согласия в ТЗ не отражены вовсе, хотя обследование — медицинское
вмешательство, а тренер — не медицинский работник.

**Решение:** три раздельных согласия, ни одно не заменяет другое.

| Согласие | Основание | Блокирует |
|---|---|---|
| ИДС на медицинское вмешательство | ФЗ-323 ст. 20; Приказ МЗ № 390н | выход сессии из `prepared` (валидатор §8.1.9а) |
| Участие в исследовании | ЛЭК; ГОСТ Р ИСО 14155 | включение сессии в когорту |
| Передача сведений тренеру | ФЗ-323 ст. 13, врачебная тайна | выдачу тренерского варианта отчёта |

Сами документы хранятся у хоста (это ПДн), в модуле — признаки и реквизиты.

**Отзыв согласия** (ФЗ-152 ст. 9 ч. 2) исполняется без разрушения исследовательского
массива: хост уничтожает соответствие `patient_ref ↔ учётная запись`, после чего данные
модуля обезличены — соотнести их с лицом невозможно. Обработка ПДн прекращена,
накопленные наблюдения сохранены. Восстановление соответствия невозможно: ключ
уничтожается, а не архивируется.

Это прямая отдача от [Р-9](#р-9--требования-к-patient_ref): решение, принятое ради
чистоты границы с хостом, оказалось механизмом исполнения отзыва согласия.

**Правки в ТЗ:** §8.1 (валидатор 9а), §15, §16. В контуре — новый §5а.

---

## Р-25 · Статус медизделия и класс риска

**Дата:** 2026-08-16
**Решение:** `intended_use` — регуляторная граница, а не флаг функциональности.

- **`research`** — ПО исключительно для научных исследований медицинским изделием
  не является, регистрации не требует. Условие: никаких выводов, применяемых к
  конкретному пациенту в диагностике или лечении. Движок правил §11 выключен, что
  уже обеспечено [Р-19](#р-19--intended_use-и-условие-загрузки-правил).
- **`clinical`** — медизделие (ФЗ-323 ст. 38), требуется государственная регистрация
  (ПП РФ № 1416; Решение Совета ЕЭК № 46). Классификация вида — Приказ МЗ РФ № 4н.

MUST: сборка не стартует в `clinical` без настроенного номера регистрационного
удостоверения. Режим отображается в интерфейсе и печатается в каждом отчёте.

MUST: назначение формулируется письменно **до** разработки и по ходу не меняется —
от него зависят класс риска и объём испытаний. Расширение назначения после регистрации
требует внесения изменений в РУ.

Класс потенциального риска (ожидаемо 2а или 2б) определяется до Этапа 6.

**Правки в ТЗ:** §1, §17 (Этап 6).

---

## Р-26 · Этическая экспертиза и ГОСТ Р ИСО 14155

**Дата:** 2026-08-16
**Решение:** до включения первого участника — **одобрение локального этического комитета**
медицинской организации; протокол, форма согласия и план анализа подаются в ЛЭК.
Набор до одобрения недопустим.

Методическая опора исследовательской части — **ГОСТ Р ИСО 14155** (клинические
исследования медизделий, надлежащая клиническая практика). Его требования
к прослеживаемости данных уже выражены в исследовательском контуре как журнал
обратимых исключений, предрегистрация и воспроизводимость анализа — то есть
дополнительной работы почти не создают.

MUST: предрегистрация гипотезы одобрение ЛЭК **не заменяет** и его аналогом
не является: первое защищает от подгонки, второе — от вреда участнику.

**Правки:** [исследовательский контур](./research-framework.md), новый §5а.

---

## Р-27 · Стандарты жизненного цикла в приёмку

**Дата:** 2026-08-16
**Проблема:** регистрация медизделия опирается на документированные процессы
разработки. Восстановить их задним числом нельзя, а §16 ТЗ описывал только уровни
тестового покрытия.

**Решение:** с Этапа 1 ведутся:

| Стандарт | Артефакт |
|---|---|
| ГОСТ Р МЭК 62304 | матрица прослеживаемости требование → тест → реализация; класс безопасности ПО; перечень SOUP с версиями и обоснованием |
| ГОСТ Р ИСО 14971 | файл менеджмента риска: опасные ситуации, меры снижения, остаточный риск |
| ГОСТ Р ИСО 13485 | документированные процедуры разработки и выпуска версий |
| ГОСТ Р 59921 (серия) | применяется **только** если вывод начнёт формировать модель |

Класс безопасности по 62304 определяется до Этапа 6; ожидаемо **класс B** — решение
принимает врач, а не система.

**Побочная выгода уже принятого решения.** [Р-19](#р-19--intended_use-и-условие-загрузки-правил)
удерживает вывод за детерминированным движком правил, а LLM — только за формулировкой
уже принятого решения. Это сознательно сужает применимость требований к системам ИИ
и заметно упрощает регистрацию. Менять порядок «правила решают, модель формулирует»
нельзя без пересмотра регуляторной стратегии.

**Правки в ТЗ:** §16.


---

## Р-28 · Проходы сессии и цикл probes_running ⇄ imported

**Дата:** 2026-08-16
**Находки:** [П-1](./review-consistency.md#п-1-подтверждающий-повтор-невозможен-в-односторонней-машине),
[П-5](./review-consistency.md#п-5-слепота-разбора-снимается-выбором-топ-2-для-повтора).

**Проблема:** §8.3 требует после полного прохода повторно измерить топ-2 пробы. Чтобы
их выбрать, нужны посчитанные Δ; значит после расчёта снова выполняются пробы.
Машина §8.1 линейна и односторонняя ([Р-1](#р-1--состояние-сессии--одно-поле)) —
вернуться из `imported` в `probes_running` нельзя. Ключевой методический элемент ТЗ
не выразим в его собственной машине состояний.

**Решение:**

1. Проба несёт `pass_no`. Подтверждающий повтор — **второй проход**, не отдельная фаза.
2. Разрешён цикл `probes_running ⇄ imported`, пока не исчерпано запланированное число
   проходов. Все прочие переходы остаются односторонними.
3. Промежуточный расчёт помечен `interim`: в `analyses` не пишется, в отчёт не попадает,
   состояние сессии не меняет. `analyzed` по-прежнему достигается один раз.
4. `POST /sessions/{id}/interim` выдаёт оператору **только упорядоченный перечень кодов
   проб к повтору** — без величин, размеров эффекта и направления. Иначе слепота разбора
   качества ([Р-17](#р-17--слепой-разбор-качества)) снимается до его закрытия: тот, кто
   видит величины откликов, уже знает, какая проба «сработала».

**Правки в ТЗ:** §4 (`trials.pass_no`), §8.1 (цикл, промежуточный расчёт), §8.3, §13.

---

## Р-29 · План сессии и отклонения от плана

**Дата:** 2026-08-16
**Находки:** [П-3](./review-consistency.md#п-3-валидаторы-не-привязаны-к-переходам-часть-срабатывает-когда-поздно),
[П-6](./review-consistency.md#п-6-sham-и-контрольные-пробы-не-имеют-места-в-порядке-групп),
[П-7](./review-consistency.md#п-7-идс-проверяется-на-неявном-переходе).

**Проблема:** валидаторы полноты и порядка (наличие sham, три нейтрали, порядок групп,
washout) проверяемы только когда список проб полон — то есть когда обследование
закончено и пациент ушёл. Блокирующая проверка, срабатывающая в этот момент, ничего
не предотвращает: она лишь помечает сессию негодной постфактум.

**Решение:** введены сущности `session_plans` и `protocol_deviations`, состояние
`plan_approved` между `prepared` и `baseline_recorded`.

1. План — заранее объявленный список проб с ролями, порядком и точками нейтрали.
   До утверждения плана не выполняется ни одна проба.
2. Валидаторы полноты и порядка проверяются **на утверждении плана**, до первой пробы.
3. Фактические пробы сверяются с планом; расхождения пишутся в `protocol_deviations`
   с причиной. Это же закрывает требование прослеживаемости ГОСТ Р ИСО 14155,
   то есть работа не добавочная, а перенесённая.
4. **ИДС проверяется при создании сессии**, а не на неявном переходе: согласие
   предшествует вмешательству, а не сопровождает его. Без признака действующего
   согласия сессия не создаётся.
5. **Sham вставляется внутрь имитируемой группы**, его позиция рандомизируется наравне
   с активными пробами. Фиксированная позиция вычисляется и пациентом, и оператором,
   и смысл sham исчезает. Контрольные пробы, не являющиеся sham, отнесены к `diagnostic`.

**Правки в ТЗ:** §4 (две таблицы), §8.1 (состояние, валидаторы, sham), §13 (`/plan`).

⚠ **расширение:** `session_plans` и `protocol_deviations` — новые сущности.

---

## Р-30 · Дрейф нейтрали в единицах SDC

**Дата:** 2026-08-16
**Находки:** [П-2](./review-consistency.md#п-2-дрейф-нейтрали-считается-через-индекс-которого-может-не-быть),
[П-10](./review-consistency.md#п-10-дрейф-одновременно-корректируется-и-штрафуется).

**Проблема:** §8.2 считал `baseline_drift` через `PI`, который требует норм. По
[Р-13](#р-13--индекс-отзывчивости-ri-работа-без-норм) норм может не быть — и тогда
контроль валидности сессии молча отключается ровно в основном режиме платформы.
Заменить на `RI` нельзя: он определён относительно `baseline_ref`, а дрейф — свойство
самих нейтралей; получается круг.

**Решение:**

```
baseline_drift = max_p |value(нейтраль_последняя)_p − value(нейтраль_первая)_p| / SDC_p
```

Порог — `thresholds.drift`, по умолчанию 1,0. Считается в пространстве параметров,
норм не требует, обоих индексов не использует. До появления собственного SDC не
работает вовсе — что согласуется с общим правилом «до Этапа 1.5 значимость
не оценивается» ([Р-32](#р-32--скелет-данных-в-этап-1)).

Отдельно разведены поправка и штраф: поправка на позицию во времени применяется всегда,
`low_confidence` выставляется по **остатку после поправки**. Иначе сессия штрафуется
за величину, которую сама же скомпенсировала. Исходный дрейф печатается справочно.

**Правки в ТЗ:** §8.2.

---

## Р-31 · Матрица «валидатор → переход»

**Дата:** 2026-08-16
**Находка:** [П-3](./review-consistency.md#п-3-валидаторы-не-привязаны-к-переходам-часть-срабатывает-когда-поздно).

**Проблема:** §8.1 давал плоский список валидаторов с пометкой «блокируют переход»,
не указывая какой именно. После [Р-7](#р-7--эндпоинты-переходов-состояния) часть
переходов стала неявной, и блокировать их нечем — факт уже произошёл.

**Решение:** каждый валидатор привязан к конкретной точке. Категории:

| Категория | Точка проверки | Валидаторы |
|---|---|---|
| Право и идентичность | создание сессии | 1, 9а |
| Полнота и порядок плана | `prepared → plan_approved` | 2, 7, 8, 9, 10 |
| Готовность к записи | `plan_approved → baseline_recorded` | 5 |
| Исполнение | приём каждой пробы | 3, 4, 6 |
| Дисциплина разбора | `imported → quality_reviewed` | 11 + сверка с планом |

Принцип: **валидатор, срабатывающий после ухода пациента, ничего не предотвращает** —
такие проверки перенесены на утверждение плана.

**Правки в ТЗ:** §8.1, список заменён таблицей.

---

## Р-32 · Скелет данных в Этап 1

**Дата:** 2026-08-16
**Находка:** [П-4](./review-consistency.md#п-4-циклическая-зависимость-этапов-1--15--2).

**Проблема:** Этапу 1.5 (режим повторяемости) нужен протокол — сессии, пробы, нейтрали, —
а это Этап 2. Этапу 1 нужен `RI`, нормированный на SDC, а SDC даёт Этап 1.5. Круг:
1 → 1.5 → 2 → 1.5.

**Решение:** разделены **скелет данных** и **машинерия протокола**.

| Этап | Содержание |
|---|---|
| 1 | сущности `sessions`, `trials`, `measurements`, `raw_imports`; ведение сессии вручную, без мастера и валидаторов; канонизация; Δ от нейтрали **в натуральных единицах**; `RI` не считается |
| 1.5 | режим повторяемости на этом скелете → ICC, SEM, SDC → с этого момента `RI` и любые суждения о значимости |
| 2 | мастер, план сессии, валидаторы, рандомизация, sham, проходы, слепой разбор, шорт-лист, предрегистрация |

Формулировка «Этап 1 → работающий продукт» сохраняется, но уточняется: продукт собирает
и показывает измерения и **не выносит суждений о значимости**. До собственного SDC
выносить их не на чем, и притворяться обратным опаснее, чем признать.

**Правки в ТЗ:** §17 (Этапы 1, 1.5, 2).

---

## Р-33 · Мелкие согласования

**Дата:** 2026-08-16
**Находки:** [П-8](./review-consistency.md#п-8-тренерский-режим-в-исследовательском-контуре),
[П-11](./review-consistency.md#п-11-компенсация-длины-ног-против-порядка-групп),
[П-12](./review-consistency.md#п-12-роли-и-варианты-отчёта-не-совпадают),
[П-13](./review-consistency.md#п-13-второй-вариант-отчёта-повторно-ставит-reported).

**Решения:**

1. **Тренерский и пациентский отчёты в режиме `research` не выпускаются.** Оба содержат
   предписания — программа на 12 недель, «чего избегать», сроки повторного сканирования, —
   а режим `research` терапевтических выводов не даёт
   ([Р-19](#р-19--intended_use-и-условие-загрузки-правил),
   [Р-25](#р-25--статус-медизделия-и-класс-риска)). Отключения движка правил
   недостаточно: тренерский отчёт выдавал бы предписания и шаблоном. Доступны
   `doctor` и `dentist` — фактологические.

2. **Компенсация длины ног — условие сессии, а не проба.** Устанавливается
   в `platform_config_baseline` до первой пробы, действует на все, включая нейтраль;
   порядку групп §8.1.2 не противоречит. Подальные пробы — корригирующие элементы
   поверх неё.

3. **Матрица «роль → допустимые варианты отчёта»** задана явно: `patient` и `dentist`
   были вариантами без ролей, `methodologist` и `operator` — ролями без вариантов.
   Методолог получает обезличенные варианты, оператор — никаких, тренер — только свой
   и только при согласии ([Р-24](#р-24--согласия-идс-исследование-передача-тренеру)).

4. **Статус `reported` выставляется при выпуске первого отчёта.** Вариантов четыре,
   и при односторонней машине повторный вызов был бы ошибкой либо молчаливым no-op;
   теперь это явно определено как допустимое действие, не меняющее состояние.

**Правки в ТЗ:** §8.1, §13.

---

## Р-34 · Автопереанализ и хранение analyses

**Дата:** 2026-08-16
**Находка:** [П-9](./review-consistency.md#п-9-автопереанализ-и-неограниченный-рост-analyses).

**Проблема:** [Р-2](#р-2--переанализ-при-смене-версий) сделал `analyses` только-на-вставку,
[Р-3](#р-3--пять-версий-конфигурации) довёл число версий до пяти,
[Р-18](#р-18--направление-unknown) гарантирует частые изменения `registry_version` по мере
выхода параметров из `unknown`. Каждое изменение разрывает сопоставимость §12 и требует
переанализа. Переанализ нигде не назван автоматическим, а объём `analyses` растёт как
«сессии × версии» без границы.

**Решение:**

1. Публикация новой версии любой из пяти конфигураций **ставит задачу переанализа**
   всех сессий (очередь §3). История §12 всегда располагает свежим общим набором версий,
   и правило сопоставимости не деградирует само собой.
2. Политика хранения: строки `analyses`, вытесненные более свежими, **не подписанные**
   и **не входящие ни в одну зафиксированную когорту**, подлежат вытеснению
   по конфигурируемому правилу. Подписанные и когортные хранятся всегда — этого требуют
   §1 (проверяемость подписанного заключения) и §7 исследовательского контура
   (воспроизводимость когортного результата).

**Правки в ТЗ:** §13.

⚠ **расширение:** автоматическая постановка переанализа и правило вытеснения — сверх
исходного §13.


---

## Р-35 · Аналитический слой в ТЗ

**Дата:** 2026-08-16
**Источник:** предложения из [разбора логики](./review-consistency.md#3-аналитика--предложение),
приняты целиком.

**Решение:** аналитика внесена в ТЗ как §9.8а, четырьмя слоями с разными сроками.

| Слой | Содержание | Этап |
|---|---|---|
| 1. Метрология | ICC, SEM, SDC, CV, паспорт параметра, разброс между операторами, эталонный доброволец | 1.5 |
| 2. Аналитика протокола | калибровка `settle_sec` и `carryover_min` по данным, урожайность протокола, сводка отклонений | **2** |
| 3. Аналитика отклика | многомерный тест, оси отклика, иерархические модели, типология отзывчивости, доза–эффект | 7 |
| 4. Мета-аналитика | выход параметров из `unknown`, судьба гипотез, калькулятор мощности | 7 |

**Два переноса против исходного §17.**

Слой 1 обязателен и совпадает с Этапом 1.5 — он и так там был, добавлены ICC, CV
и разброс между операторами. Последний важнее, чем кажется: при нескольких операторах
это часто крупнейший источник дисперсии, и если он сравним с эффектом пробы, все выводы
недостоверны независимо от статистики.

**Слой 2 перенесён в Этап 2** (в исходном плане аналитика целиком была Этапом 7).
Калибровка выдержки и отмывки по собственным данным сокращает длительность сессии,
а значит и стоимость набора. Отложенная на конец, она не успевает повлиять на протокол,
ради разработки которого платформа и строится. Снапшот когорты — туда же: без него
слой 3 невоспроизводим.

**Главное содержательное решение — многомерность.** Основной единицей анализа отклика
объявлен вектор, а не набор скаляров. Набор одномерных тестов с поправкой на
множественность теряет мощность именно на небольших согласованных сдвигах — а поза есть
связанная система, и параметры ожидаемо движутся вместе. То есть стандартный подход
подавляет ровно тот сигнал, который платформа ищет. Одномерные тесты остаются, но рядом,
а не вместо.

**Правки в ТЗ:** §9.8а (новый), §17 (Этапы 1.5, 2, 7).

⚠ **расширение:** весь раздел — новый объём работ.


---

## Р-36 · Реальные протоколы приборов, реестр 2026.2

**Дата:** 2026-08-16
**Источник:** протоколы клиники — DIERS formetric 4D `Parameter_F4_Dynamic4D`
(04.03.2025, условие «лев окк», 3 км/ч) и CADIAX 4 (30.11.2023, движения
«Открывание/закрывание» и «Протрузия/ретрузия»).

Первый контакт с настоящими выгрузками приборов. Он же — исполнение MUST §5.2:
на каждый парсер лежит реальный файл и ожидаемый разбор.

**Решения:**

1. **Единица сагиттального дисбаланса исправлена: мм → градусы.** Прибор печатает
   «5° A». Правка меняет результат при тех же измерениях, поэтому это **новая
   версия реестра (2026.2)**, а не редактирование существующей — ровно тот случай,
   ради которого [Р-3](#р-3--пять-версий-конфигурации) вводил `registry_version`
   в `input_hash` и в сопоставимость §12.

2. **Динамика — отдельные коды `DYN_*`.** Максимум за цикл шага при 3 км/ч и
   статическое значение того же угла — разные измеряемые величины под одним
   названием. Смешивать нельзя. Размах движения заведён отдельным параметром,
   его границы — служебными, вне индекса. Скорость дорожки — условие пробы,
   а не измерение.

3. **Кондилография — домен `condylar`, `in_pi: false`.** §9.5 разводит позу,
   сустав и мышцу как три отдельных сигнала; суммировать их в один индекс
   значило бы стереть саму конструкцию когерентности. Сторона кодируется
   суффиксом `_R`/`_L`, как у оси ноги.

4. **Метка условия разрешается реестром, а не парсером.** «лев окк» → алиас
   в реестре проб. **`MAND_OCCLUSION_LEFT` НЕ приравнена к `MAND_LAT_LEFT`:**
   окклюзионное условие и латеротрузионная проба — разные вещи, и отождествить
   их в коде значило бы предрешить результат исследования (Р-18 о том же).

5. **Сверка экскурсии (§6.2) получила источник величины.** CADIAX печатает
   QUANTITY / Maximum excursion distance — это и есть `excursion_mm` для сверки
   с формометрической пробой, допуск ±1,0 мм. Реализовано `match_excursion`.

**Две ловушки настоящего протокола, обе в тестах:**

- **Сторона задана словом, а не знаком:** «17 мм Прав.», «1° Лев.». Приведение
  к канону (вправо положительно) живёт в знаковых соглашениях §5.3.
- **Границы размаха печатаются в порядке фазы цикла, а не по возрастанию:**
  «20 мм Прав. - 6 мм Прав.». Наивное «первое — минимум» даёт размах −14.

**Что заметила сама платформа:** воспроизводимость CADIAX на открывании 0,46 мм
справа против 0,03 мм слева — пятнадцатикратная разница, автоматический флаг
`reproducibility_side_mismatch`. До интерпретации асимметрии SCI (+23,4 % на 10 мм)
следует исключить, что её объясняет неравное качество записи.

**Сопоставимость:** протоколы относятся к разным визитам (2023 и 2025).
По §12 разности между ними не вычисляются; загружены как две отдельные сессии.

**Персональные данные:** заголовок окна CADIAX содержит ФИО пациента. В репозиторий
не перенесено ничего: фикстуры обезличены, `patient_ref` непроизводный (Р-9),
исходные файлы вне системы контроля версий. Это тот случай, ради которого
приняты Р-9 и Р-11.

**Открыто, требует ответа клиники:**

1. «лев окк» — это отдельное окклюзионное условие или синоним латеротрузии влево?
   Сейчас разведены.
2. Знак «A» в сагиттальном дисбалансе прочитан как anterior → плюс. Встречается ли
   «P», и как он печатается?
3. Существует ли машинный экспорт CADIAX и DICAM (XML/CSV)? Разбор печатной формы
   зависит от раскладки версии печати и остаётся резервным путём, а не основным.

**Правки:** новый `param_registry_2026.2.yaml` и `profile_2026.2.yaml`, реестр проб,
парсеры `formetric_pdf` и `cadiax_numeric`, `match_excursion` и `condylar_asymmetry`
в домене, экран «Приборы».

⚠ **расширение:** две новые модальности и связанные с ними коды.


---

## Р-37 · Стыковка модальностей и классификация проб

**Дата:** 2026-08-16
**Запрос:** пользователь подгружает формометрию, ЭМГ и кондилографию; система сама
сопоставляет пробы, стыкует визуализацию и выделяет лучшие, худшие и нейтральные.

**Решения:**

1. **Реестр 2026.3: ЭМГ и myoline.** Панель ВНЧС (жевательная, височная, ГКС,
   трапециевидная) плюс постуральная цепь, по сторонам, с производной асимметрией.
   Оба домена — `in_pi: false`: §9.5 держит мышцу третьим сигналом наравне с позой
   и суставом, а не слагаемым индекса. Myoline (сила) — по §9.8 MUST в анализе проб
   не участвует и привязан к **сессии**, а не к пробе: это характеристика человека,
   а не отклик на условие.

2. **Три сигнала на пробу и когерентность.** `synthesize_probe` считает Δ позы (RI),
   изменение асимметрии мыщелков и ΔMI, сводя их не в сумму, а в согласованность.
   Единственное место, где направление берётся из методики, а не из данных, —
   суставной сигнал: §9.5 сам определяет его как симметризацию.

3. **Классификация проб.** `best` / `worse` / `conflicting` / `unsigned` / `neutral` /
   `no_posture`. Два значения существуют потому, что без них платформа врала бы:
   - **`unsigned`** — отклик достоверен по величине, но ни один изменившийся параметр
     не имеет установленного направления (Р-18). Назначить знак нечем.
   - **`no_posture`** — под пробой нет формометрии. Свалить это в «нейтрально»
     значит выдать отсутствие данных за результат.

   MUST: классификация описывает **измеренный отклик**, а не назначение. В режиме
   `research` движок правил выключен (Р-19), и переход от «даёт наибольшее улучшение
   позы» к «назначить эту пробу» делает врач.

4. **Автоподгрузка.** `POST /sessions/{id}/auto-ingest` принимает пачку файлов без
   указания модальности и пробы: формат определяется через `detect()`, метка условия
   читается из файла, разрешается алиасами реестра, измерение подшивается к пробе
   или заводит её, пробы сортируются по порядку групп §8.1.2.

   **Метка, которой нет в реестре, не угадывается**: файл разобран, статус
   `needs_probe_assignment`, назначение — за человеком. Нераспознанный формат
   остаётся с причиной (MUST §5.1).

5. **Визуализация трёх модальностей.** ЭМГ — зеркальные каналы, асимметрия читается
   формой; кондилография — профиль SCI по амплитудам, расхождение кривых сторон и есть
   суставной сигнал; формометрия — прежняя полоса ±SDC. Сильная асимметрия ЭМГ
   подсвечивается **обводкой, а не цветом**: направление «лучше/хуже» для неё
   не установлено.

**Два дефекта, найденных тестами:**

- `detect()` ЭМГ-парсера перехватывал формометрический CSV: маркером считалось слово
  `RMS`, а заголовок формометрии содержит «Lateral Deviation **rms**». Признаком
  формата сделаны распознанные каналы — канал требует и мышцу, и сторону
  одновременно, случайно совпасть почти не может. Регрессия закреплена тестом.
- Фикстура тестов грузила реестр 2026.1, тогда как приложение работает на 2026.3;
  тест когерентности проходил вхолостую, потому что ЭМГ-параметров в старом реестре
  нет. Фикстура приведена к текущей версии, старая оставлена для проверок §12.

**Правки:** реестр 2026.3 и профиль 2026.3, `importers/emg_csv.py`,
`domain/crossmodal.py`, роутеры `ingest` и `crossmodal`, экраны «Загрузка»
и «Сравнение», компоненты визуализации. 89 тестов.

⚠ **расширение:** две модальности, автоподгрузка и классификация проб —
новый объём работ.


---

## Р-38 · Три раскладки и обе темы (§14.6)

**Дата:** 2026-08-16
**Требование:** §14.6 ТЗ — компьютер с боковой навигацией, планшет до 1024 с
горизонтальной, телефон до 700 с вертикальными строками и таблицами, которые
прокручиваются, не пряча колонок; светлая и тёмная темы плюс режим «авто».

**Аудит показал два нарушения, а не косметику:**

1. **Страница прокручивалась вбок на телефоне и планшете** — прямое нарушение
   §14.2: тело страницы не должно ездить горизонтально. Причина оказалась не там,
   где ожидалось: не таблицы (у них прокрутка была), а ряд сегментов выбора пробы.
   Восемь кнопок не помещались и выталкивали страницу. То же воспроизвелось
   и на **узком ноутбуке 1112 px**, где действует десктопная раскладка: боковая
   панель забирает 260 px. Поэтому правило прокрутки сегментов сделано общим,
   а не вложенным в медиазапрос.

2. **Переключатель темы был недоступен на телефоне и планшете.** Он жил в боковой
   панели, а она на этих ширинах скрывалась целиком — сменить тему было нечем.
   Добавлена шапка с брендом и компактным переключателем, появляющаяся ровно там,
   где исчезает боковая панель.

**Три раскладки:**

| Ширина | Навигация | Содержимое |
|---|---|---|
| > 1024 | боковая панель 260 px | сетка плиток, полные подписи |
| 701…1024 | горизонтальный ряд под шапкой | сетка сохраняется, подписи вкладок сокращены |
| ≤ 700 | нижняя панель вкладок | плитки вертикальными строками, селекторы на всю ширину |

**Короткие подписи вкладок — не стиль, а вместимость.** Семь полных названий
на 390 px не помещаются: панель прокручивалась к активной вкладке, и пользователь
видел один раздел вместо семи. С короткими подписями (`Файлы`, `Пробы`, `Повтор`,
`Метод`, `Реестр`) видны все семь на любой из проверенных ширин.

**Графики получили собственную прокрутку** с минимальной шириной на телефоне: сжатие
SVG до ширины экрана делает подписи нечитаемыми. Это тот же принцип, что §14.2 п. 7
задаёт для таблиц — прокручивать, а не прятать.

**Проверка:** 12 сочетаний (6 ширин × 2 темы), автоматически, по трём критериям —
все семь вкладок в пределах панели, переключатель темы виден, боковой прокрутки нет.
Ширины: 390×844, 844×390, 834×1112, 1112×834, 1440×900, 1920×1080.

**Правки:** `styles.css` (три брейкпоинта вместо одного), `App.tsx` (шапка,
короткие подписи), `components/Modality.tsx` (левое поле графика — подпись
обрезалась при уменьшении).


---

## Р-39 · Обзор «от сустава до стоп» и сравнение набора проб

**Дата:** 2026-08-16
**Запрос:** сравнивать в одной таблице произвольное число проб; видеть
сопоставленные **графики**, а не только цифры, по областям — позвоночник, таз,
ноги, голени, стопы — как в отчёте прибора.

Это §14.3 ТЗ, который до сих пор не был реализован: обзор строками сверху вниз
от ВНЧС до стоп, где модальности стоят рядом, а не по отдельным экранам.

**Решения:**

1. **Анатомическая раскладка — конфигурация ОТОБРАЖЕНИЯ, вне пяти версий.**
   `config/regions/anatomy.yaml` не входит в `input_hash` и своей версии в наборе
   Р-3 не имеет. Обоснование: группировка параметров по областям тела не меняет
   ни одного вычисляемого значения — ни z, ни RI, ни размера эффекта. Включить её
   в хэш значило бы объявлять переанализ всех сессий каждый раз, когда кто-то
   переименовал заголовок раздела.

   Порядок областей задан §14.3 и закреплён тестом: он воспроизводит логику
   осмотра, а не удобство вёрстки.

2. **Каждый параметр обязан иметь область.** Тест падает, если появился
   параметр без неё: иначе данные есть, но в обзор не попадают, и пользователь
   об этом не узнает. При первом прогоне тест поймал 14 таких — границы размаха
   и изометрическую силу.

3. **Мышцы распределены по обслуживаемым областям**, а не сложены в одну
   «ЭМГ»: жевательная — к ВНЧС, выпрямитель спины — к сагиттали, ягодичная —
   к тазу, икроножная — к голеням. Без этого мышечный сигнал нельзя сопоставить
   с позой в той же строке, ради чего обзор и существует.

4. **Сравнение произвольного набора проб.** `GET /sessions/{id}/compare?probes=…`
   отдаёт одну таблицу: строки — параметры по областям, столбцы — выбранные пробы.
   Цвет серии кодирует **пробу**, направление уходит в подпись — §14.2 п. 3 ровно
   для случая нескольких наложенных объектов. По умолчанию сравниваются лучшая
   и худшая: этот контраст читают первым.

5. **Схема тела кодирует ВЕЛИЧИНУ отклика, а не направление.** Насыщенность
   области — максимальный по модулю эффект среди выбранных проб. Красить область
   в «хорошо/плохо» нельзя: направление большинства параметров не установлено
   (Р-18), и такая заливка была бы враньём в самом заметном месте экрана.

6. **Структурные области выводятся без Δ** (§14.2 п. 8): ось ног и изометрическая
   сила показывают абсолютные значения — отсчитывать их от нейтрали бессмысленно.

**Дефект, найденный при проверке:** ключ снимка данных зависел от порядка проб
в запросе, и один и тот же набор давал разные адреса. Порядок канонизирован
сортировкой на стороне клиента.

**Правки:** `config/regions/anatomy.yaml`, `domain/config.py` (`load_anatomy`),
`routers/crossmodal.py` (`/compare`), `components/BodyMap.tsx`,
`screens/CrossModal.tsx` (режимы «одна проба» и «сравнить несколько»).
95 тестов.

⚠ **расширение:** анатомическая раскладка и режим сравнения набора проб.

---

## Р-40 — Иллюстрации протокола и угловой перекос таза

**Повод.** Сравнение в разделе «Проба» не показывало больше трёх проб, а в
загруженном отчёте DIERS остались данные, до которых разбор не доходил.

### 1. Ограничение в три пробы было в снимке, а не в приложении

Приложение ограничений на число проб не имело никогда: `/compare` принимает
любой набор. Ограничивал автономный снимок — он заранее раскладывал ответы по
комбинациям проб, а комбинаций экспоненциально много, и генератор обрывался на
трёх. Снаружи это выглядело как предел приложения.

Починка не в увеличении предела, а в снятии самой развилки: `/compare` без
параметра `probes` отдаёт **все** пробы сессии, а столбцы фильтруются на
клиенте. Снимку теперь нужен **один ключ на сессию** вместо комбинаторного
перебора, и предел исчезает вместе с причиной.

Урок общего свойства: предзаготовка ответов под каждое сочетание фильтров
всегда даёт молчаливый предел. Отдавать надо полный набор и фильтровать на
клиенте — тогда предела нет и объяснять нечего.

### 2. «Перекос таза» — две разные величины под одной подписью

В одном и том же листе протокола Dynamic4D прибор печатает:

| где | значение | величина |
|---|---|---|
| таблица | 17 мм Прав. (размах 6…20 мм) | линейная разность высот DL-DR |
| иллюстрация | 9° R (размах 0° Лев. … 11° Прав.) | угол линии таза к горизонтали |

Это не расхождение прибора и не ошибка печати: величины разные, совпадает
только русская подпись. Сведение их в один код испортило бы динамику
необратимо — 17 и 9 стали бы значениями одной величины.

**Решение.** Реестр **2026.4**: заведены `PELVIC_OBLIQUITY_ANGLE`,
`DYN_PELVIC_OBLIQUITY_ANGLE` и его границы размаха, отдельно от миллиметровых
кодов. Пересчёт между ними не делается ни в какую сторону: он требует ширины
таза, которой протокол не печатает, а подстановка «типичной» ширины дала бы
угол, которого никто не измерял. Тест `test_pelvic_obliquity_mm_and_deg_are_different_params`
защищает разделение от будущего «упрощения».

Заодно исправлено то, что вскрылось попутно: приложение по умолчанию работало
на реестре **2026.1**, хотя 2026.2 и 2026.3 уже существовали. ЭМГ,
кондилография и динамика при настройках по умолчанию попадали в анализ
безымянными кодами с доменом `unknown`. Умолчания подняты до 2026.4, пороги —
до `demo-2026.4`, где появились демонстрационные SDC для `DYN_*` и `CDG_*`.

### 3. Иллюстрации протокола хранятся, а не выбрасываются

Печатный протокол несёт растровые вложения: реконструкцию поверхности спины,
схему таза с линией перекоса, схему таза сверху. Часть значений напечатана
**только** на них — растром, вне текстового слоя. Выбросить их значило бы
потерять данные молча, что запрещено §5.1 ровно так же, как для неканонических
столбцов.

`ParseResult.figures` + таблица `import_figures`; классификация по геометрии
(`render` / `caption` / `decor`), а не по догадке о содержании. При наличии
подписи разбор поднимает флаг `figure_values_not_in_text_layer` — оператор
видит, что на листе есть числа, которых нет в разборе.

**OCR сознательно не делается.** Значение, полученное распознаванием, не имеет
прослеживаемости к прибору, а §5 требует источник и знак для каждого числа.
Вместо этого — явная ручная транскрипция с отдельной записью импорта
(`format_id="manual-figure-transcription-v1"`, флаг
`manually_transcribed_from_figure`) и **сверкой**: коды, которые есть и в
разборе, и в транскрипции, обязаны совпасть. На реальных данных сверка
проходит по ротации таза (5° и там, и там) и подтверждает, что транскрипция
верна там, где её проверить нечем.

### 4. Схемы строятся из измерений, иллюстрации показываются рядом

Печатная иллюстрация существует для одной пробы одного визита — сравнить по ней
четыре пробы нельзя. Поэтому та же геометрия строится из параметров
(`components/Anatomy.tsx`): угол проводится под измеренным углом, размах
движения — веером от границы до границы, доля опоры — насыщенностью. В режиме
сравнения раскладка «строка = вид, столбцы = пробы»: сравнивают один вид между
пробами, и рядом должны стоять одинаковые схемы.

**Форма кривой позвоночника не рисуется.** Протокол её не выгружает, и вместо
правдоподобной S-образной кривой показан коридор ±max отклонения с меткой rms.
Нарисованная кривая была бы формой, которой в данных нет, — и читалась бы как
измерение.

**Правки:** реестр `2026.4`, профиль `2026.4`, пороги `demo-2026.4`,
`regions/anatomy.yaml`, `importers/base.py` (`Figure`), `importers/formetric_pdf.py`
(`classify_figure`, `_figures`), `models.py` (`ImportFigure`),
`services/figures.py`, `routers/figures.py`, `routers/imports.py` (+`.pdf`),
`routers/ingest.py`, `routers/crossmodal.py`, `ingest_real.py` (транскрипция со
сверкой), `settings.py`, `.env.example`, `components/Anatomy.tsx`,
`components/ProtocolFigures.tsx`, `screens/CrossModal.tsx`, `screens/Instrument.tsx`,
`lib/api.ts`, `styles.css`, `tools/build_snapshot.py`, `tools/build_standalone.py`.
102 теста.

⚠ **расширение:** хранение иллюстраций протокола и анатомические схемы.

---

## Р-41 — Myoline измеряется под пробой и входит в анализ четвёртым сигналом

**Повод.** В протоколе клиники одноимённые пробы ставятся на всех четырёх
приборах: формометрия, ЭМГ, кондилография и myoline — с одним и тем же
положением нижней челюсти. ТЗ же трактовало силу как характеристику сессии.

**Что было неверно.** §9.8 требовал: «myoline не участвует в анализе проб;
используется только для дозирования программы». Для протокола, где сила
измеряется ПОД условием, это стирает ровно тот эффект, ради которого измерение
и делается: одно число на сессию усредняет отклик по всем пробам сразу.
Формулировка отменена, введён §9.8б.

### Что изменилось

1. **Сила привязывается к пробе по метке условия**, как остальные модальности.
   Парсер разрезает одну выгрузку на пробы: клиника выгружает все условия
   подряд, и резать файл руками оператор не должен.

2. **Выгрузка без метки условия не отвергается.** Архив у клиники есть, и
   терять его из-за отсутствия столбца нельзя. Такой файл остаётся сессионной
   ковариатой с флагом `myoline_session_level_no_condition` — различие видно,
   а не спрятано. Проба `MYOLINE_SESSION` сохранена как корзина именно для них.

3. **`SI` — сводный индекс силы**, устроен как `MI`: ньютоны разных мышечных
   групп несравнимы напрямую, в единицах собственного SDC — сравнимы. В `PI`/`RI`
   не суммируется: складывать ньютоны с миллиметрами бессмысленно.

4. **`MYO_ASYM_TRUNK_LAT`** — асимметрия боковой силы по той же формуле
   200·(R−L)/(|R|+|L|), что асимметрия ЭМГ и мыщелков. Три «асимметрии» с разной
   шкалой под одним словом читались бы как одна величина. В `SI` не входит: она
   безразмерна и имеет собственный порог, а сумма «уровня» с «перекосом» дала бы
   величину, рост которой нельзя истолковать.

### Чего сила НЕ делает — и почему это главное решение

**Сила не голосует в когерентности и не влияет на вердикт**, пока `direction`
у `MYO_FORCE_*` равен `unknown`.

Соблазн был обратный: «больше силы — лучше» выглядит очевидным. Но под пробой
это не установлено. Рост усилия при смещённой челюсти может означать и лучшую
опору, и компенсаторное перенапряжение — то есть ровно противоположные вещи,
и различить их может только исследование, а не арифметика. Записать «больше —
лучше» в код значило бы подтвердить эту гипотезу самим фактом её реализации:
любая проба с приростом силы автоматически получала бы плюс к согласованности,
и проверить исходное допущение стало бы уже нечем.

Поэтому голосование включается **сменой `direction` в реестре**, а не правкой
кода: как только направление подтверждено собственными данными, сила начинает
голосовать, и запись об этом остаётся в версии конфигурации. Это тот же
механизм, что для любого другого параметра (Р-18).

Показывается сила при этом наравне с остальными: собственная полоса на панели
сигналов, схема в ряду сравнения проб, `ΔSI` рядом с `ΔPI` и `ΔMI` на экране
анализа, строки в таблице «от сустава до стоп». Нейтральным цветом — величина
есть, знака нет.

**Обобщение когерентности.** `full` перестало означать «ровно два из двух» и
означает «все голосующие сигналы сошлись, и их не меньше двух». Иначе появление
четвёртого сигнала молча сделало бы `full` недостижимым.

### Демонстрационные данные

Сид раньше порождал только формометрию — четыре сигнала было негде увидеть.
Теперь под каждой пробой генерируются ЭМГ и myoline, включая сжатие зубов с
классическим конфликтом §9.5: поза хуже, сила выше. Это тот случай, ради
которого сигналы и держат раздельно.

**Правки:** реестр `2026.5`, профиль `2026.5`, пороги `demo-2026.5`,
`regions/anatomy.yaml` (сила перестала быть структурной областью),
`importers/emg_csv.py` (`MyolineCsvParser` по условиям, `add_lateral_asymmetry`),
`domain/crossmodal.py` (`strength_index`, четвёртый сигнал),
`domain/effects.py` (`coherence` с голосованием, `ProbeResponse.delta_muscle/_strength`),
`domain/analysis.py`, `routers/ingest.py`, `routers/crossmodal.py`, `seed.py`,
`settings.py`, `.env.example`, `components/Modality.tsx` (`SignalPanel`),
`components/Anatomy.tsx` (`StrengthBars`), `screens/CrossModal.tsx`,
`screens/SessionView.tsx`, `lib/api.ts`. ТЗ: §9.5, §9.8а, §9.8б. 110 тестов.

---

## Р-43 — QA загрузки реального отчёта: три дефекта

**Повод.** «При загрузке реального отчёта DIERS происходит ошибка.» Проверка
пути загрузки нашла три дефекта, из которых один был опаснее сообщённого.

### 1. Молчаливая порча многостраничного отчёта (главный)

Настоящий отчёт — 41 страница: «Parameter_F4_Dynamic4D» повторяется для каждого
условия («лев окк», «прав окк», «нейтраль»). Парсер разбирал документ **целиком
как один раздел**: метку условия брал от первого заголовка, а значения — от
последнего, потому что одноимённые строки перетирали друг друга.

Ошибки при этом не возникало. Данные всех условий сливались в одну пробу,
подписанную чужой меткой, и результат выглядел правдоподобно. Это худший вид
отказа: заметить его можно только сверив числа с бумагой.

**Исправлено:** разбор режет отчёт на разделы по заголовку — раздел на условие,
каждый со своей меткой, своими значениями и своими иллюстрациями (страница
отдаёт растры первому своему разделу). Автоподгрузка раскладывает разделы по
пробам, как раскладывала отдельные файлы. Флаг `multi_section_protocol:N`
делает деление видимым.

**Ручная загрузка такой файл теперь отклоняет** (409). Она привязывает файл к
ОДНОЙ пробе, и сложить в неё разделы разных условий значило бы повторить ту же
порчу на уровень выше. Причина отказа называет найденные условия.

### 2. Ошибка загрузки в автономном снимке (сообщённый симптом)

Опубликованная страница — снимок без бэкенда. `autoIngest` шёл в сеть мимо
снимка, получал от прокси HTML-страницу 404, а `res.json()` падал на ней с
`SyntaxError: Unexpected token '<'`. Пользователь видел это вместо объяснения.

**Исправлено:** снимок распознаётся до запроса, и экран загрузки говорит прямо,
что файлы принимать некуда. Ответ, оказавшийся не JSON, больше не роняет
разбор: сообщается код ответа и то, что запрос, похоже, не дошёл до API.

Правило общее: экран, который ПИШЕТ, обязан проверять, есть ли куда писать.
Предлагать действие, которое не может выполниться, — дефект интерфейса, а не
пользователя.

### 3. ПДн в журнале аудита

Клиника называет выгрузки по пациенту («Петрова Жанна.pdf»). Имя файла уходило
в `audit_log` — то есть ПДн оседали в постоянном хранилище модуля вопреки Р-9 и
ФЗ-152. **Исправлено:** в журнал пишутся расширение и размер; файл всё равно
лежит по хешу, и для разбора инцидента этого достаточно.

Попутно: причина отказа перестала быть именем класса исключения.
«PdfStreamError: Stream has ended unexpectedly» заменено на «файл повреждён или
скачан не полностью» — класс сохранён в скобках для журнала.

---

## Р-42 — Каталог мышц ЭМГ, шаблоны монтажей и ручная пропись каналов

**Повод.** Записываемый состав мышц задаётся протоколом исследования, а не
платформой: нужны типовые шаблоны по группам и возможность вписать каналы
руками.

### Три списка, которые расходились

Мышцы были заданы в трёх местах сразу: коды — в реестре параметров, алиасы
меток — словарём в парсере, принадлежность области тела — в раскладке §14.3.
Каждое пополнение требовало трёх согласованных правок, и они расходились.

**Источник теперь один** — `packages/config/emg/muscles.yaml`. Из него
порождаются коды реестра (`tools/gen_emg_registry.py`), берётся область тела
(`load_anatomy` подмешивает мышцы в раскладку) и распознаются метки каналов.
Каталог расширен с 10 мышц до 40, из них 37 доступны поверхностно.

`surface: false` — мышца в каталоге есть, но поверхностными электродами не
снимается (латеральная крыловидная, поперечная живота, подвздошно-поясничная).
Такой код не попадает ни в реестр, ни в монтаж: подписать им канал значило бы
объявить измеренным то, чего этот метод не измеряет. В интерфейсе они показаны
отдельным списком с причиной — иначе оператор искал бы пропавшую мышцу.

### Монтаж: что БЫЛО записано

Каталог говорит, какие мышцы бывают. **Монтаж сессии** говорит, что записано в
этой сессии и каким проводом. Разница практическая: миограф подписывает каналы
«CH1…CH8», и по имени столбца мышцу не восстановить ничем. Выходов ровно два —
оставить канал неопознанным или взять пропись оператора. Догадка («третий канал
обычно ГКС») выходом не является: она даёт измерение, которого никто не делал.

Десять шаблонов по группам: панель ВНЧС, шейно-плечевая, лопаточно-плечевая,
разгибатели и сгибатели туловища, пояснично-тазовая, бедро, голень и стопа,
постуральная цепь целиком и прежний десятиканальный — для совместимости с уже
загруженными сессиями, оставленный неизменным намеренно.

Шаблон — заготовка: он разворачивается в список каналов, который правится и по
метке, и по составу. Проверки при сохранении не формальность:
- **метка дважды** — при разборе одна запись перетёрла бы другую; сохранение
  блокируется, и конфликтные строки подсвечены;
- **односторонний отвод** — законен (пишут, когда интересует сторона поражения),
  но асимметрии по нему не будет, и сказать об этом надо при настройке, а не
  пустой колонкой в отчёте.

### Почему монтаж применяется ПОСЛЕ парсера

Парсер обязан оставаться чистой функцией от байтов: тот же файл — тот же разбор
всегда, иначе воспроизводимость §7 начинает зависеть от состояния сессии.
Монтаж — знание оператора, а не свойство файла, поэтому он применяется
отдельным шагом и оставляет собственный след во флагах качества
(`channels_from_montage:N`, `montage:<шаблон>`).

По той же причине монтаж **не участвует в первом заходе распознавания**: метки
вроде «CH1» перетягивали бы на себя посторонние таблицы, где два таких столбца
найдутся легко. Второй заход делается только для файла, который не опознал
никто, — и тогда назначение однозначно.

**Правки:** `config/emg/muscles.yaml`, `config/emg/montages.yaml`, реестр
`2026.6`, профиль `2026.6`, пороги `demo-2026.6`, `regions/anatomy.yaml`
(мышечные списки убраны, области `shoulder`/`abdomen`/`thigh` добавлены),
`domain/config.py` (`load_muscles`, `load_montages`, подмешивание в раскладку),
`domain/montage.py`, `importers/emg_csv.py` (`resolve_channel` по каталогу,
разбор с монтажом), `models.py` (`session_montages`), `routers/montage.py`,
`routers/ingest.py`, `tools/gen_emg_registry.py`, `screens/Montage.tsx`,
`App.tsx`, `lib/api.ts`, `styles.css`. 128 тестов.

⚠ **расширение:** каталог мышц, монтажи и экран монтажа.

---

## Р-44 — Беспроводные датчики ЭМГ и снимки экрана проводного миографа

**Повод.** Проверить канал подключения датчиков по Bluetooth и распознавание
снимков экрана с проводного ЭМГ.

**Результат проверки: ни того, ни другого не было.** Ни строки про BLE, serial
или потоковый приём; снимок экрана ручная загрузка отвергала по расширению, а
автоподгрузка — как нераспознанный формат. То есть данные проводного миографа,
у которого выгрузки нет вовсе, терялись целиком.

### Датчики по BLE

**Соединение ведёт браузер, расчёт — сервер.** BLE — локальная радиосвязь между
датчиком и машиной оператора; у сервера в стойке радио нет, и тянуть датчик туда
значило бы держать оператора у стойки. Но на сервер уходят **отсчёты и метки
времени**, а не готовые RMS: расчёт §9.8 обязан быть воспроизводимым, и версия
браузера в него входить не должна. Клиент делает ровно то, чего сервер сделать
не может — принимает пакеты, — и ни грамма арифметики сверх распаковки байтов.

**Профиль GATT — данные, а не код** (`config/devices/ble_profiles.yaml`): UUID
сервиса, характеристика, упаковка отсчётов, порядок каналов, коэффициент к
микровольтам. Зашить это в код значило бы требовать сборки под каждый датчик.

**`verified: false` у всех профилей**, и это не оговорка. Профили описаны по
документации, железа для сверки нет, а ошибка в коэффициенте к микровольтам
даёт правдоподобные, но неверные амплитуды — худший вид ошибки в измерительном
тракте. Каждая запись по непроверенному профилю несёт флаг
`ble_profile_unverified`; снять его можно только сверкой с проводным трактом на
одном сигнале.

**Что канал ловит честно:** фактическую частоту против заявленной
(`resample_suspected`) и потерю пакетов (`packet_loss:N`). Пересчитать поток к
заявленной частоте было бы легко и неверно: это скрыло бы рваную запись.

**Исправленная собственная ошибка.** Первая версия считала «SNR» как отношение
оставшегося в полосе к отфильтрованному. На чистой записи с сильной сетевой
наводкой такая величина уходит в минус — канал выглядел негодным именно потому,
что фильтр отработал. Величина переименована в `mains_share_db` (доля
выброшенного трактом: наводка и контакт электрода), а SNR §9.8 считается
**только по записанной эпохе покоя**. Без покоя он не определён, и это сказано
флагом `snr_not_assessable`, а не подставленным числом.

### Снимки экрана

Принимаются и хранятся как **свидетельство**: изображение кладётся к пробе
иллюстрацией, тип экрана определяется по геометрии и палитре (`trace` —
тёмная широкая развёртка, `report` — светлый лист, `unknown` — не похоже ни на
то, ни на другое, и форма не предлагается).

**Числа с растра не распознаются, и это решение, а не нехватка библиотеки.**
§5.2а требует источника и знака для каждого значения; у величины, полученной
распознаванием, происхождения нет, сверить её не с чем, а ошибка «8» вместо «3»
даёт правдоподобное измерение, которого никто не делал. Числа со снимка вводит
человек — тем же механизмом ручной транскрипции со сверкой, что и подписи на
иллюстрациях протокола DIERS (Р-40).

Отдельно: OCR потребовал бы внешнего сервиса (запрещено ФЗ-152 ст. 12 и Р-23) —
либо движка в образе. Второе возможно и ничего не меняет: запрет методический.

---

## Р-45 — Свои шаблоны монтажа и две раскладки просмотра проб

**Повод.** Составлять шаблоны групп мышц вручную; смотреть пробы и схемы
вертикально и горизонтально, несколько проб одновременно.

### Свои шаблоны

Собираются из групп мышц: выбор по областям тела, кнопка «вся группа», сторона
на выбор («обе» даёт два отвода и асимметрию; односторонний выбор законен, но
асимметрии по такой мышце не будет — сказано при составлении).

**Хранятся отдельно от поставляемых** (`montage_templates` против
`config/emg/montages.yaml`). Слить их значило бы либо терять пользовательские
при обновлении, либо позволить правку поставляемых — после которой уже
загруженные сессии ссылались бы на изменившийся состав. Перекрыть код
поставляемого шаблона нельзя: отказ с причиной.

**Удаление — архивирование.** На шаблон ссылаются прошлые сессии, и стереть его
значило бы потерять запись о том, чем они сняты.

### Две раскладки

Раскладка отвечает на разные вопросы, и одна на оба не отвечает:

- **горизонтально** — строка на вид, столбцы пробы: сравнение ОДНОГО показателя
  между пробами;
- **вертикально** — колонка на пробу, виды сверху вниз: чтение пробы целиком.

Переключатель стоит и в сравнении проб, и на экране приборных данных; там же
добавлен выбор нескольких проб сразу. Число проб не ограничено — ряд
прокручивается, страница вбок не едет.

**Правки:** `config/devices/ble_profiles.yaml`, `domain/emg_stream.py`,
`domain/config.py` (`load_ble_profiles`), `domain/montage.py`
(`montage_from_channels`), `importers/screenshot.py`, `models.py`
(`montage_templates`), `routers/devices.py`, `routers/montage.py`,
`routers/imports.py` (изображения), `components/BleCapture.tsx`,
`components/Anatomy.tsx` (`orientation`), `screens/Montage.tsx`,
`screens/Instrument.tsx`, `screens/CrossModal.tsx`, `lib/api.ts`, `styles.css`,
`tools/build_snapshot.py`, `tools/build_standalone.py`. 142 теста.

⚠ **расширение:** канал датчиков, приём снимков, свои шаблоны, раскладки.

---

## Р-46 — Отчёт прибора показывается как напечатан; таз, стопы и колени по каждой пробе

**Повод.** «Перекос таза и ротация таза не заполнены из отчёта, стопы тоже для
каждой пробы… я хочу видеть именно эти картинки и цифры как в отчёте, не меняй
визуализацию DIERS из отчёта.»

### Пустые схемы были пустыми данными, а не сломанной отрисовкой

Проверка: в демо-пробах лежали три тазовых параметра из пяти, а подометрии и оси
ног не было вовсе. Схема таза честно писала «ротация таза не измерена», схема
стоп — «подометрия не загружена». Отрисовка работала правильно; порождать было
нечего.

**Исправлено в порождении демо-данных:** добавлены `PELVIC_ROTATION` и
`PELVIC_OBLIQUITY_ANGLE`, подометрия (доли опоры и центр давления) на каждую
пробу, ось ног — на сессию. Доли опоры считаются согласованно и дают в сумме
100 %: шуметь ими независимо значило бы нарушить определение величины, а не
добавить погрешность прибора.

**Ось ног структурна** (§14.2 п. 8): измеряется один раз и по пробам не
меняется. В данные она кладётся один раз, а в схему каждой пробы подмешивается
при показе. Записать её измерением в каждую пробу было бы проще и неверно: это
объявило бы сделанными измерения, которых не делали.

Добавлена схема коленей — излом оси голени относительно оси бедра, с отвесом
как отсчётом. Колени были в реестре и в области `leg_axis`, но вида не имели.

### Печатная иллюстрация — главное изображение, а не миниатюра

Раньше картинки отчёта шли полоской миниатюр НИЖЕ собственных схем платформы.
Это была ошибка приоритета: врач читает отчёт прибора, а не пересказ.

Теперь иллюстрации протокола идут **первыми, крупно, сгруппированные так, как
напечатаны**: схема и следующие за ней подписи-растры принадлежат одному виду.
Порядок берётся из самого PDF — сначала схема, потом её значения; другого
способа связать подпись со схемой нет, координат у растра в текстовом слое не
бывает. На листе Petrova это даёт ровно то, что на бумаге: таз с линией и
«Перекос таза: 9° R, 0° Лев. — 11° Прав.», таз сверху и «Ротация таза: 5° R,
1° L — 7° R», реконструкция спины.

Собственные схемы остались, но названы своим именем — «схемы из измерений» — и
подписаны: это не пересказ отчёта, а другой инструмент. Печатная иллюстрация
существует для одной пробы одного визита; сравнить по ней четыре пробы нельзя,
ради этого схемы и строятся из чисел.

**Правки:** `seed.py` (таз целиком, подометрия, ось ног), `components/Anatomy.tsx`
(`KneeAxis`), `components/ProtocolFigures.tsx` (группировка как на листе),
`screens/Instrument.tsx`, `screens/CrossModal.tsx` (порядок блоков, подмешивание
структурных величин), `styles.css`. 142 теста.

---

## Р-47 — Иллюстрации берутся из отчёта DIERS, а не перерисовываются

**Требование автора:** «иллюстрации стоп, позвоночника и других структур должны
быть взяты полностью из DIERS», «не меняй визуализацию DIERS из отчёта».

**Что было неверно.** Платформа рисовала свои схемы и показывала их как основное
изображение пробы; печатные иллюстрации шли ниже. Это подмена: врач читает отчёт
прибора, а не пересказ. Приоритет перевёрнут.

### Структура определяется печатным заголовком, а не пикселями

Чтобы разложить иллюстрации отчёта по разделам — позвоночник, таз, колени,
стопы, — надо знать, о чём картинка. Есть два пути, и они не равноценны:

- по изображению («круглое серое — таз») — догадка, и в отчёте она была бы
  неотличима от факта;
- по печатному заголовку страницы — прибор сам называет, о чём лист.

Взят второй. `structures_in()` ищет в тексте страницы слова, которыми DIERS
называет структуру, и все иллюстрации этой страницы наследуют результат. В
полном отчёте лист обычно про одну структуру, и совпадение точное.

**Лист, называющий несколько структур сразу, показывается в каждом разделе — и
об этом сказано текстом.** Разносить его иллюстрации «по смыслу» значило бы
вернуться к догадке. Страница, не назвавшая структуру вовсе, попадает в
«прочие», а не раскладывается по виду картинки.

### Чего нет — то так и написано

Раздел без листа отчёта показывает «в загруженных страницах отчёта иллюстрации
этой структуры нет», а не свою схему. Подставить туда перерисовку значило бы
выдать чертёж за изображение прибора — ровно то, что требование запрещает.

Собственные схемы остались, но переименованы в «схемы из чисел», убраны под
кнопку и выключены по умолчанию. Они нужны там, где листа отчёта нет, и для
сравнения проб между собой: печатная иллюстрация существует для одной пробы
одного визита.

**Сравнение проб теперь идёт по иллюстрациям отчёта:** строка — структура,
столбцы — пробы (или наоборот при вертикальной раскладке). Пустая ячейка честно
говорит «нет листа в отчёте».

**Правки:** `importers/base.py` (`Figure.structures`, `Figure.page`),
`importers/formetric_pdf.py` (`STRUCTURE_WORDS`, `structures_in`), `models.py`,
`services/figures.py`, `routers/figures.py`, `components/ReportViews.tsx`,
`screens/Instrument.tsx`, `screens/CrossModal.tsx`, `lib/api.ts`, `styles.css`.
144 теста.

---

## Р-48 — Печатный отчёт myoline «Comparison Agonist/Antagonist»

**Повод.** Загружен реальный отчёт myoline от 04.06.2025.

**Что это за формат.** Не таблица сил, а сопоставление агониста с антагонистом:
девять пар движений по пяти областям — туловище, шея, ноги, предплечье, плечо, —
к каждой паре сила в ньютонах, процент норматива прибора и пиктограмма
работающей мышцы. CSV-парсер myoline такого не читает: это другой документ.

Разобрано 45 величин: 18 сил, 18 процентов, 3 асимметрии сторон и 6 отношений
агонист/антагонист. Реестр — **2026.7**.

### Три решения внутри разбора

1. **Пара называет свои движения сама.** Строка «Туловище| Разгибание /
   Сгибание» задаёт область и состав, а значения привязываются ПО ИМЕНИ
   движения, а не по порядку строк. Другая версия печати с переставленным
   порядком иначе поменяла бы стороны местами — левое стало бы правым, и
   ошибка была бы неотличима от биологии.

2. **Процент — норматив ПРИБОРА, не наша норма.** «252 N / 78 %» означает 78 %
   от норматива DIERS. Платформа работает без норм (Р-13), и подставить чужую
   шкалу на их место значило бы считать «отклонение от нормы» по величине,
   которую мы не проверяли. Проценты живут отдельным семейством `MYO_PCT_*`,
   в индексы не входят, и каждая запись несёт флаг
   `device_normative_percent_not_platform_norms`.

3. **Отношение агониста к антагонисту считается по силам, а не по процентам.**
   Отношение процентов измеряло бы шкалу прибора, а не пациента.

### ПДн: файл в репозиторий не кладётся

Отчёт печатает ФИО и дату рождения. Парсер отбрасывает такие строки до того,
как что-либо попадёт в `unmapped` (иначе имя осело бы там под видом
«неканонической строки»), — но **сам файл остаётся носителем ПДн**, и место
ему не в git. Подключается локально через `DIERS_MYOLINE_REPORT`; тесты
работают на синтетическом листе с той же раскладкой. `.gitignore` закрывает
`tests/fixtures/*.pdf` целиком.

Отчёт снят у другого человека, чем формометрия, поэтому загружен под отдельным
`patient_ref`: свести их в одного пациента значило бы объявить связь, которой
в данных нет.

### Найденный при этом дефект

**Парсер формометрии забирал ЛЮБОЙ PDF** — `detect` проверял только сигнатуру
`%PDF`. Отчёт myoline попадал к нему первым и падал с «не протокол
Parameter_F4_*» вместо того, чтобы достаться своему парсеру. Формат теперь
опознаётся по тому, что прибор напечатал, а не по обёртке файла.

**Пороги по масштабу области.** Шея выдаёт 65–84 Н, отведение ног — 3856 Н.
Единый demo-порог сделал бы шейные пробы вечно «в шуме», а ножные — вечно
значимыми, поэтому порог назначен по области. Значения назначены, не измерены.

**Правки:** `importers/myoline_pdf.py`, `importers/formetric_pdf.py` (detect),
реестр `2026.7`, профиль `2026.7`, пороги `demo-2026.7`, `regions/anatomy.yaml`,
`probes/probe_registry.yaml` (алиас «нейтр»), `ingest_real.py`,
`components/ReportViews.tsx` (раздел «Изометрическая сила», свёрнутые пустые
разделы), `tools/build_snapshot.py`, `.gitignore`. 149 тестов.

---

## Р-49 — Отчёт подсвечивает, какое положение челюсти значимо меняет позу

**Повод.** Главный вопрос протокола приходилось собирать глазами из раскладки по
вердиктам. При десятке проб это работа, которую делает читатель, а не платформа.

Блок вынесен в начало сравнения проб и в анализ сессии: мандибулярные пробы,
отсортированные по |Δ| постурального отклика, с разбором — какие параметры
сдвинулись сильнее всего и сходятся ли остальные сигналы.

### «Значимо» здесь означает ровно одно

|Δ| постурального отклика не меньше порога SDC — сдвиг больше собственного
разброса повторного измерения. Не «клинически важно» и не «показано к
назначению».

**Порог назван рядом числом.** Слово «значимо» без порога — риторика: читатель
не может проверить утверждение. Для этого `threshold` добавлен и в ответ
`/crossmodal`, и в `AnalysisResult`. Когда пороги демонстрационные, над блоком
стоит баннер: «значимо» тогда означает превышение демо-порога, а не
установленной различимости прибора.

### Три графы вместо двух

Проба, достоверно сдвинувшая позу, но не имеющая ни одного параметра с
подтверждённым направлением, попадает в **«меняет позу, знак не установлен»**, а
не в улучшения или ухудшения (Р-18). Разница не формальная: именно в этой графе
живут закономерности, которых ещё никто не назвал, и приписать им знак значило
бы либо потерять их, либо выдать догадку за результат.

Отдельно различаются «в пределах шума» и «поза не измерена»: первое — результат,
второе — отсутствие данных.

### Контрольная проба помечена

Sham — челюстная проба, и в список она входит, но с меткой «контроль». Сдвиг под
ней говорит о методе, а не о лечебном положении: аппарат без терапевтического
позиционирования двигает позу сам по себе, и на эту величину нужно поправлять
остальные пробы. Если такая проба оказалась среди сдвинувших позу, блок говорит
об этом отдельной строкой — иначе её легко прочитать как терапевтическую находку.

Клин под пятку и прочие немандибулярные пробы в список не идут: они отвечают на
другой вопрос. Отбор по группе пробы из реестра, а не по префиксу кода.

**Правки:** `domain/analysis.py` (`AnalysisResult.threshold`),
`routers/crossmodal.py` (`group`, `role`, `threshold`, `thresholds_are_demo`),
`components/MandibleImpact.tsx`, `screens/CrossModal.tsx`, `screens/SessionView.tsx`,
`lib/api.ts`, `styles.css`. 150 тестов.
